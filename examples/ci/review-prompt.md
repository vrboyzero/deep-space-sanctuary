Review the current repository for concrete correctness, security, and regression risks.

Use only the tools granted by the runtime. If workspace writes are granted, make only a minimal, directly justified fix; otherwise keep the workspace unchanged. Never push, merge, publish, modify credentials, or access files outside the supplied workspace.

Return only one JSON object matching the supplied output schema. Keep findings actionable and reference repository-relative paths. Use an empty findings array when no issue is found.
