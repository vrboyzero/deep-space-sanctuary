version: 2

updates:
  # Keep full-SHA workflow references current through reviewable pull requests.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
