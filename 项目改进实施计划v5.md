# 项目改进实施计划 v5

更新时间：2026-05-17

## 0. 重点执行要求

由于本计划涉及面广、功能重要、跨模块影响明显，**后续每一步实际实施前，都必须先做一次“方案文档 + 对应代码”的局部再确认，不允许直接凭计划摘要开工。**

执行要求如下：

1. 实施某一项计划前，必须先回看对应的方案文档：
   - `E:\project\star-sanctuary\星辰功能优化需求.md`
   - `E:\project\star-sanctuary\指挥官Agetn实现方案.md`
   - `E:\project\star-sanctuary\token消耗优化方案v2.md`
2. 需要根据当前要推进的子项，重新检索并阅读相关代码、配置、测试与现有实现入口，确认代码现状没有漂移。
3. 只有在“方案意图、现有代码、影响范围、风险边界”都重新确认后，才能进入实现、改动与验证。
4. 若实施时发现方案文档与当前代码现状不一致，应先记录差异、收缩范围或更新子计划，再继续推进，不应直接按旧假设编码。
5. 实施改进功能时，**不得默认接受“记忆能力下降”作为代价**。如果某项优化会让记忆相关能力变差，例如：
   - 动态记忆注入 / 自动召回效果明显下降
   - `memory_search` / `memory_read` 的会话内可用性下降
   - task memory / resume context / recent work 质量下降
   - durable extraction / memory summary 的写入质量或召回质量下降
   则必须先补护栏、收缩方案，或直接暂缓该项，不应在记忆能力退化状态下继续推进。

这是一条贯穿所有阶段的强约束，适用于：

- 阶段开始前
- 子任务开始前
- 跨文件/跨模块改动前
- 高风险修复或重构前

## 1. 目标与结论

本计划用于统筹以下三份后续开发内容：

1. `E:\project\star-sanctuary\星辰功能优化需求.md`
2. `E:\project\star-sanctuary\指挥官Agetn实现方案.md`
3. `E:\project\star-sanctuary\token消耗优化方案v2.md`

结论：三份文档之间不存在根本性目标冲突，但**不能按文档顺序直接并行硬推**。更合适的推进方式是：

- 先做**运行时基础设施与低风险开关**；
- 再做**提示词可见性与指挥官治理最小闭环**；
- 再做**动态提示聚焦、深度压缩与多 Agent 长任务增强**；
- 最后处理**经验资产再合成**与**记忆数据库去重**这类高风险或运维型能力。

推荐采用：**“基础设施优先、治理与提示并行、记忆清理后置”** 的混合路线。

---

## 2. 已核对的当前代码基线

以下判断已与当前仓库代码对齐：

### 2.1 系统提示词与运行时注入

- `packages/belldandy-core/src/bin/gateway.ts:1367-1405`
  当前在 Gateway 启动时通过 `loadWorkspaceFiles()` + `buildSystemPromptResult()` 一次性构建 system prompt。
- `packages/belldandy-core/src/bin/gateway.ts:1757-1835`
  当前已存在 `agentWorkspaceCache`，说明默认 Agent 与 resident Agent 的 prompt 都是预构建并缓存的。
- `packages/belldandy-agent/src/tool-agent.ts:643-669`
  当前 `before_agent_start` hook 已可返回 `prependContext` 与 `deltas`，适合作为“提示聚焦”注入点。
- `packages/belldandy-agent/src/system-prompt.ts:340-402`
  `AGENTS.md`、`SOUL.md`、`MEMORY.md` 已按 section 形式静态注入，且有明确优先级。

### 2.2 记忆总开关与记忆运行时

- `BELLDANDY_MEMORY_ENABLED` 当前已出现在 WebChat 设置与配置白名单中，
  见 `apps/web/public/app/features/settings.js` 与 `packages/belldandy-core/src/server-methods/config-channel.ts`。
- 但 Gateway 运行时实际控制的仍是子开关：
  `BELLDANDY_CONTEXT_INJECTION`、`BELLDANDY_AUTO_RECALL_ENABLED`、
  `BELLDANDY_MEMORY_SUMMARY_ENABLED`、`BELLDANDY_MEMORY_EVOLUTION_ENABLED`、
  `BELLDANDY_TASK_MEMORY_ENABLED`、`BELLDANDY_EMBEDDING_ENABLED`，
  见 `packages/belldandy-core/src/bin/gateway.ts:1413-1424`、`2406-2434`。
- 这意味着“记忆总开关无实际控制力”的判断成立，且修复成本很低。

### 2.3 Method / Skill 资产链路

- `packages/belldandy-memory/src/experience-promoter.ts:68-70`
  当前草稿创建直接调用 `buildMethodDraft()` / `buildSkillDraft()`。
- `packages/belldandy-memory/src/experience-promoter.ts:165-290`
  两个 draft builder 仍是硬编码输出，不走模板。
- `packages/belldandy-core/src/server-methods/memory-experience.ts:1510-1532`
  当前 synthesis 模板已存在统一加载函数 `resolveExperienceSynthesisTemplate()`。
- `packages/belldandy-core/src/server-methods/memory-experience.ts:847-853`
  当前再合成仅接受 `draft` candidate，不支持已发布资产。

### 2.4 多 Agent / 指挥官底座

- `packages/belldandy-agent/src/agent-profile.ts`
  已支持 `defaultPermissionMode`、`defaultAllowedToolFamilies`、`toolWhitelist` 等字段。
- `packages/belldandy-skills/src/executor.ts:370-383`
  当前 `ToolExecutor.getDefinitions()` 是适合加入 per-agent 硬拦截的现成入口。
- `packages/belldandy-skills/src/builtin/session/delegate.ts`
  与 `packages/belldandy-skills/src/builtin/session/delegate-parallel.ts`
  说明单任务与并行委派能力已具备。

### 2.5 Token / 压缩 / 缓存链路

- `packages/belldandy-agent/src/compaction.ts:568`
  当前压缩阈值仍是固定 `12000` token。
- `packages/belldandy-agent/src/microcompact.ts:24-29`
  当前 microcompact 仅覆盖 4 个工具名。
- `packages/belldandy-agent/src/microcompact.ts:119-124`
  当前 microcompact 会**原地改写旧 tool message**，这与前缀缓存稳定性存在直接冲突。
- `packages/belldandy-agent/src/token-counter.ts`
  当前已有 token 统计服务，但还没有 USD 成本模型。
- `packages/belldandy-core/src/provider-model-catalog.ts:242-302`
  当前已有 provider/model catalog，但还没有 capability / pricing 层。

---

## 3. 三份文档的关联、冲突与优化点

### 3.1 强关联项

| 主题 | 关联文档 | 关系 |
|------|---------|------|
| Prompt 注入与注意力管理 | 星辰功能优化需求 + Token 消耗优化方案 v2 | 都会改 system prompt / 压缩 / 缓存命中率 |
| 多 Agent 治理 | 指挥官 Agent 实现方案 + Token 消耗优化方案 v2 | Commander 会放大 token 消耗，必须配合成本可观测性 |
| Method / Skill 资产可见性 | 星辰功能优化需求 + 指挥官 Agent 实现方案 | 指挥官与执行者都受益于 method/skill 摘要注入 |
| 记忆边界与污染控制 | 星辰功能优化需求 + 指挥官 Agent 实现方案 | scratch memory、数据库去重、总开关都在处理“什么应该进入长期记忆” |

### 3.2 需要显式规避的冲突

1. **Prompt 聚焦 vs 前缀缓存稳定性**
   - 如果把每轮动态命中的内容直接改写静态 system prompt，会破坏 DeepSeek 自动前缀缓存收益。
   - 结论：Prompt 聚焦必须作为**易变前缀/运行时 delta** 注入，不能重写 `agentWorkspaceCache` 中的静态 prompt。

2. **Prompt 聚焦 vs 长期记忆数据库**
   - 如果把 `AGENTS.md` / `SOUL.md` 的切片直接塞进 `memory.sqlite/chunks` 主链路，会混淆“工作区规则”和“用户长期记忆”。
   - 结论：Prompt 聚焦的索引应使用**独立命名空间或独立存储表/文件索引**，不要直接与普通记忆去重、演化、召回共池。

3. **Commander Scratch Memory vs Memory Dedup**
   - scratch 是任务临时推理痕迹，不适合进长期记忆库，更不适合参与数据库去重。
   - 结论：scratch 必须坚持文件系统 markdown 方案，不进入 SQLite。

4. **已发布资产再合成 vs 草稿模板双轨**
   - 如果不先统一 draft 生成模板，再做已发布资产再合成，会继续扩大“创建”和“合成”输出格式漂移。
   - 结论：必须先做模板复用，再做 resynthesize。

5. **多 Agent 扩张 vs 成本不可见**
   - 在没有成本追踪与阈值治理前先全面推进 Commander，多 Agent 会放大 token、压缩、重试成本。
   - 结论：至少要先有 A3/A4 级别的成本与阈值基础设施。

### 3.3 优化建议

1. Prompt 聚焦的 MVP 不要强依赖 embedding。
   - 当前 `BELLDANDY_EMBEDDING_ENABLED` 仍可能因 key 缺失被关闭。
   - 推荐先做“分段 + FTS/关键词召回 + 可选向量增强”的降级设计。

2. Method / Skill 列表摘要注入应先于 Prompt 聚焦落地。
   - 它是低风险、低工作量、立即见效的可见性增强。
   - 也能为 Commander 方案直接提供基础能力。

3. Commander 先做“硬约束 + 人工治理”，后做“auto 启发式”。
   - 先跑通 Profile、工具硬拦截、review/scratch。
   - `governanceMode=auto` 放到第二阶段，不要一开始就把复杂启发式放进主线。

4. Memory Dedup 先做 hash-only dry-run。
   - 模糊去重和辅助模型判重属于后续增强，不能作为首版交付门槛。

### 3.4 Token 优化实施前检查清单（记忆能力护栏）

以下清单用于收口 `token消耗优化方案 v2` 中最可能影响记忆能力的几个子项。分类含义：

- `可直接做`：按当前边界实施，通常不会让记忆能力下降
- `需要护栏`：可以做，但必须先补边界、回退或验证机制
- `暂缓`：在当前前置条件未满足前，不建议直接推进

| 项目 | 结论 | 实施前检查点 |
|------|------|-------------|
| `A1` 前缀缓存稳定性 | **需要护栏** | 1. 不得通过重写静态 system prompt 来实现“聚焦”或“稳定前缀”。2. `microcompact` 不能继续在缓存敏感 provider 上原地改写旧 tool message。3. 只允许把动态记忆相关内容作为 `before_agent_start` 的运行时 delta / prelude 注入。4. 验证 `MEMORY.md` 静态注入、`<recent-memory>` / `<auto-recall>` 动态注入、task memory hook 三条链路都未被破坏。 |
| `A2` 智能固定（Pinning） | **可直接做** | 1. 固定对象优先覆盖错误、补丁、关键文件路径、工具调用-结果对。2. 不要把“固定”做成对长期记忆写入格式的改写。3. 验证被固定内容不会阻断 `memory_search` / `memory_read` 的正常关联记录。 |
| `A4` 压缩阈值改为上下文窗口百分比 | **可直接做** | 1. 保留固定阈值 fallback。2. 验证大窗口模型下 compaction 触发明显后移，但 `trimMessagesToFit()` 仍不会提前挤掉当前轮动态记忆 prelude。3. 重点回归长会话下的 `<recent-memory>`、`<auto-recall>`、recent work / resume context 注入质量。 |
| `B1` 工具结果分级压缩 + 可检索性 | **需要护栏** | 1. 首版不要默认把 `memory_search` / `memory_read` / task memory 相关工具直接纳入重压缩名单。2. 若要压缩 memory 工具结果，必须同步提供 `retrieve_tool_result` 或等价恢复路径。3. 会话内压缩与 transcript 持久化要分开：不能把会话内压缩误当成 durable extraction 输入裁剪策略。4. 必须验证压缩后模型仍能复用“刚刚召回了哪些记忆”和“刚刚读了哪个 memory 文件”。 |
| `B2` 分层模型路由 | **需要护栏** | 1. 首版只作用于 compaction / 辅助摘要，不要自动扩展到 `MEMORY_SUMMARY_*`、`MEMORY_EVOLUTION_*`、`TASK_SUMMARY_*`。2. 若未来要覆盖记忆相关链路，必须单独验证摘要质量、提取质量、误召回率与漏召回率。3. 低价模型路由必须可回退、可配置、可逐项关闭。 |

补充约束：

- 若 `B1` 与 `B2` 的护栏不能同时满足，则优先 `defer`，不要先上线一个“省 token 但记忆变差”的中间态。
- 若 `A1` 实施后仍保留破坏性 `microcompact`，则视为与当前记忆/缓存边界冲突，不能算完成。
- 若后续 Prompt 聚焦实现需要复用索引，默认使用独立索引或独立命名空间，不得直接与普通 durable memory chunk 共池。

---

## 4. 推进顺序候选方案对比

### 方案 A：先做指挥官，再补成本优化

优点：

- 能最快看到多 Agent 协作效果。
- 对产品感知最强。

缺点：

- 成本与上下文治理滞后，多 Agent 会放大 prompt、压缩、工具结果带来的成本问题。
- 风险收口较弱，容易在后面返工 Prompt/Compaction 链路。

结论：**不推荐作为主路线。**

### 方案 B：先做全部 Token 优化，再做产品能力

优点：

- 运行时底座更稳。
- 后续功能都能建立在更好的成本与缓存模型之上。

缺点：

- 前期用户可感知收益偏弱。
- Method/Skill 可见性与 Commander 问题会被延后，影响业务侧推进节奏。

结论：**适合底层专项冲刺，不适合本次整体推进。**

### 方案 C：基础设施优先，然后治理与提示并行推进

优点：

- 先补总开关、成本追踪、阈值治理这些低风险基础设施。
- 随后以最小闭环推进 Method/Skill 可见性、Commander 硬约束、Prompt 聚焦。
- 把高风险的记忆去重与资产再合成放到后段，降低返工。

缺点：

- 需要更强的阶段管理，避免并行阶段互相踩线。

结论：**推荐方案。**

---

## 5. 推荐实施路线

## 阶段 0：基线与护栏

### Goal

先补所有后续阶段都会依赖的低风险运行时护栏与观测能力。

### Included

1. 实现 `BELLDANDY_MEMORY_ENABLED` 运行时总开关覆盖。
2. 为 token 统计补充 USD 成本追踪基础。
3. 把压缩阈值从固定值改为“上下文窗口百分比 + fallback 固定值”。
4. 设计并落地 `ProviderCapability` 最小骨架，先覆盖：
   - `cache`
   - `contextWindow`
   - `pricing`
   - `jsonReliability`

### Excluded

- 不在本阶段引入新的多 Agent 行为。
- 不在本阶段改写 compaction 算法主体。
- 不在本阶段处理 Method/Skill 资产链路。

### Risk

低。

主要失败模式：

- 误把 `BELLDANDY_MEMORY_ENABLED` 扩展到 `INJECT_MEMORY` 等静态 prompt 开关。
- 定价表与 provider 识别规则设计得过死，后续难扩展。

### Feasibility

高。现有代码入口明确，改动集中，易测。

### Workload

约 2-4 天。

### Done

- `BELLDANDY_MEMORY_ENABLED=false` 时，记忆相关子开关在运行时被覆盖关闭，但 `INJECT_AGENTS` / `INJECT_SOUL` / `INJECT_MEMORY` 不受影响。
- 每轮模型调用可产出 token 与 USD 成本统计。
- compaction 阈值可按 provider/model 的 contextWindow 计算。

### Validation

- Unit：gateway 配置分支测试。
- Unit：成本换算测试、provider capability 识别测试。
- Smoke：WebChat 设置面板读写后，重启 Gateway 验证总开关行为。

### Intended Effect

为后续 Prompt、Commander、多 Agent、压缩优化提供可观测、可控、可回退的底座。

---

## 阶段 1：可见性与治理最小闭环

### Goal

先解决“Agent 不知道自己有哪些经验资产”和“Commander 还没有真正硬约束”这两个最直接的问题。

### Included

1. Method / Skill 列表摘要注入 system prompt。
2. 新增 `commander` / `verifier` Agent Profile。
3. 在 `ToolExecutor.getDefinitions()` 增加 Commander 工具硬拦截。
4. 将 Commander 的最小运行模式固定为：
   - `permissionMode=plan`
   - 只读 + goal + delegation 工具
   - 禁止 `workspace-write` / `patch` / `command-exec`

### Excluded

- 不在本阶段做 Task Scratch Memory。
- 不在本阶段做 `governanceMode=auto`。
- 不在本阶段做 Prompt 聚焦。

### Risk

低到中。

主要失败模式：

- 方法/技能摘要过长，导致 system prompt 截断压力上升。
- Commander whitelist 与硬拦截判定重复但不一致。

### Feasibility

高。当前系统 prompt section 与 tool definition 过滤入口都已存在。

### Workload

约 3-5 天。

### Done

- Agent 在不主动调用 `method_list` / `skills_list` 的情况下，也能看见可用经验资产摘要。
- Commander 无法获取写工具，即使 whitelist 误配也无效。
- Verifier 可作为只读验收角色被委派。

### Validation

- Unit：system prompt section 注入测试。
- Unit：executor per-agent hard block 测试。
- Integration：`delegate_task` / `delegate_parallel` 下 Commander → coder / verifier 的基本链路可跑通。

### Intended Effect

先把“知道可用资产”和“不会越权写代码”这两个基础问题解决，再进入更复杂的动态提示与长任务治理。

---

## 阶段 2：Prompt 精准化与缓存稳定性

### Goal

在不破坏前缀缓存收益的前提下，提升系统提示词的注意力命中率。

### Included

1. 引入前缀缓存稳定性模型。
2. 把 microcompact 从“原地改写旧消息”调整为更稳的策略：
   - 追加式摘要，或
   - 至少在缓存敏感 provider 上禁止破坏性改写。
3. 实现智能固定（Pinning）：
   - 错误
   - 补丁
   - 关键文件路径
   - 工具调用-结果对
4. 实现 Prompt 聚焦 MVP：
   - 以 `before_agent_start` 为注入点
   - 仅聚焦 `AGENTS.md` / `SOUL.md`
   - 用独立索引，不与普通记忆共池
   - 先支持 lexical / FTS 召回，向量检索作为增强

### Excluded

- 不在本阶段直接把 Prompt 聚焦依赖到 memory.sqlite 主检索链路。
- 不在本阶段做 B4 缓存对齐摘要。

### Risk

中。

主要失败模式：

- Prompt 聚焦注入内容过于易变，反而降低缓存命中。
- 聚焦命中不准，生成噪音上下文。
- microcompact 行为调整后影响现有长对话稳定性。

### Feasibility

中高。Hook 点现成，但需要谨慎处理静态 prompt 与动态 delta 的边界。

### Workload

约 5-8 天。

### Done

- Prompt 聚焦不会改写 `agentWorkspaceCache` 中的静态 system prompt。
- 命中段落可作为运行时 delta 注入。
- 缓存敏感 provider 上不再因为旧消息原地改写而持续失效。

### Validation

- Unit：聚焦分段、召回、阈值、注入上限测试。
- Integration：Prompt Snapshot 对比静态前缀与动态 delta。
- Manual：DeepSeek 路线下观察缓存相关 usage 字段与提示质量。

### Intended Effect

在不明显拉高 token 成本的前提下，提高长系统规则对当前任务的可用性。

---

## 阶段 3：Commander 长任务工作流补全

### Goal

把“指挥官”从 Agent 配置提升为真正可用的长任务治理模式。

### Included

1. Task Scratch Memory（文件系统 markdown）。
2. Commander Review / Lessons Learned 模板。
3. Goal / Node / Run / Agent 的隔离键绑定落盘。
4. Commander 派发、审查、返工、验收闭环。
5. 运行时治理模式字段：
   - `executionMode`
   - `governanceMode`
   - `commanderAgentId`
   - `preferredAgents`

### Excluded

- 不在本阶段做云端记忆。
- `governanceMode=auto` 只做最小启发式，不做复杂策略引擎。

### Risk

中。

主要失败模式：

- scratch 与长期记忆边界不清，造成污染。
- review 文档太重，反而拖慢任务执行。
- Node / Run 绑定不一致，导致溯源困难。

### Feasibility

高。长任务、委派、resident agent、会话隔离都已有底座。

### Workload

约 1-2 周。

### Done

- Commander 能基于现有 delegate / goal 工具完成规划、分工、复审。
- scratch 文件不进入 SQLite。
- 每次 Run 至少保留 work order、review、lessons learned 的最小记录。

### Validation

- Integration：一个跨模块任务的 commander → coder/researcher/verifier 全链路演练。
- Manual：检查产物目录、scratch、review 是否符合预期。

### Intended Effect

把当前已有的多 Agent 底座真正转化为可管理、可审查、可积累的长期任务工作流。

---

## 阶段 4：经验资产生产与再合成统一

### Goal

统一 Method / Skill 草稿的生成规范，补齐已发布资产更新链路。

### Included

1. `buildMethodDraft()` / `buildSkillDraft()` 改为优先复用 synthesis 模板。
2. 保留当前硬编码逻辑作为 fallback。
3. 支持已发布 Method / Skill 反序列化为 virtual candidate。
4. 支持基于已发布资产发起 resynthesize preview / create。
5. 补最小 UI 或方法管理入口。

### Excluded

- 不在本阶段做大规模资产批量重写。
- 不自动覆盖旧文件，仍保持 draft → review → accept。

### Risk

中。

主要失败模式：

- LLM 生成的 draft 与当前发布格式不一致。
- 已发布资产反序列化信息不全，造成 preview 偏差。

### Feasibility

中高。模板加载已存在，主要是桥接与状态放宽。

### Workload

约 1 周。

### Done

- 新建 draft 与 synthesis 使用同一模板规范。
- 已发布资产可发起更新型再合成，但默认仍走人工审阅。

### Validation

- Unit：template reuse 路径测试。
- Integration：published asset → preview → synthesize → accept 流程测试。

### Intended Effect

避免经验资产格式漂移，让“创建”和“再合成”收敛到同一套内容规范。

---

## 阶段 5：深度 Token 优化

### Goal

在基础成本可见、缓存模型稳定后，再做高收益但更复杂的压缩与恢复优化。

### Included

1. B1：工具结果分级压缩 + 可检索性。
2. B3：工具调用修复管线。
3. B4：缓存对齐摘要路径。
4. B2：分层模型路由。
5. C1：更精确的 tokenizer。
6. C3：迭代预算警告系统。

### Excluded

- C2 使用日志压缩与 C4 子智能体蒸馏指标可放到后续补充版。

### Risk

中到高。

主要失败模式：

- 压缩策略过激导致信息丢失。
- 修复管线误修复，反而引入行为偏差。
- 分层路由在便宜模型上生成低质量摘要。

### Feasibility

中。需要建立在前面 ProviderCapability、缓存稳定性和成本追踪已完成的前提上。

### Workload

约 1-3 周。

### Done

- 大输出工具可被更细粒度压缩并支持按需重取。
- 缓存能力 provider 上可以走缓存对齐摘要。
- 复杂或异常工具调用场景可减少重复 token 浪费。

### Validation

- Unit：压缩器、修复器、budget 警告逻辑测试。
- Integration：大工具输出、重复调用、JSON 截断恢复测试。
- Manual：不同 provider 路线下对比 usage 与成本表现。

### Intended Effect

把 token 优化从“基础观测”推进到“实际显著节省成本且不损失可用性”。

---

## 阶段 6：记忆数据库去重

### Goal

在记忆边界、scratch 策略、经验资产流转都稳定后，再进行数据库去重治理。

### Included

1. `memory.sqlite` 自动备份。
2. hash-only exact dedup dry-run。
3. hash-only exact dedup apply。
4. 报告输出。
5. fuzzy dedup 作为可选增强开关，默认关闭。

### Excluded

- 首版不做全自动辅助模型模糊去重。
- 首版不在 WebChat 中做复杂批量运维入口。

### Risk

高。

主要失败模式：

- 误删有意义差异。
- 索引重建不完整。
- 用户误以为这是“无损清理”。

### Feasibility

中。技术实现不难，但回滚与验证要求高。

### Workload

约 4-7 天。

### Done

- dry-run 能给出明确报告。
- apply 只处理 exact duplicate。
- 去重后 FTS / 向量索引可恢复一致。

### Validation

- Manual：备份恢复演练。
- Integration：dedup 前后查询结果对比抽样。

### Intended Effect

在不污染主业务推进节奏的前提下，逐步收缩历史记忆膨胀风险。

---

## 6. 推荐实际推进顺序

按实际执行版顺序，建议这样排：

1. 阶段 0：基线与护栏
2. 阶段 1：可见性与治理最小闭环
3. 阶段 2：Prompt 精准化与缓存稳定性
4. 阶段 3：Commander 长任务工作流补全
5. 阶段 4：经验资产生产与再合成统一
6. 阶段 5：深度 Token 优化
7. 阶段 6：记忆数据库去重

这条主线保持串行，不建议把阶段 1 与阶段 2 当成正式并行落地项。两者都会改 prompt / runtime 可见面，串行推进更容易收口风险并减少返工。

### 6.1 阶段级执行顺序

| 顺序 | 阶段 | 是否可直接启动 | 说明 |
|------|------|----------------|------|
| 1 | 阶段 0：基线与护栏 | **可直接启动** | 所有后续阶段的观测、成本、阈值、总开关底座，必须先完成。 |
| 2 | 阶段 1：可见性与治理最小闭环 | **阶段 0 完成后启动** | 先解决 Agent 资产可见性与 Commander 越权问题。 |
| 3 | 阶段 2：Prompt 精准化与缓存稳定性 | **阶段 1 完成后启动** | 先有治理边界，再处理 Prompt 聚焦与缓存稳定性。 |
| 4 | 阶段 3：Commander 长任务工作流补全 | **阶段 2 基本稳定后启动** | 避免在 Prompt / 缓存边界未稳时放大多 Agent 长任务复杂度。 |
| 5 | 阶段 4：经验资产生产与再合成统一 | **阶段 3 后启动** | 不阻塞主链路，但会改 Method / Skill 资产口径，放后面更稳。 |
| 6 | 阶段 5：深度 Token 优化 | **阶段 4 后启动** | 拆成低风险先行、高风险后置，不要一次性全开。 |
| 7 | 阶段 6：记忆数据库去重 | **最后启动** | 运维型、高回滚敏感项，必须后置。 |

### 6.2 各阶段内部执行顺序

#### 阶段 0：基线与护栏

1. `BELLDANDY_MEMORY_ENABLED` 运行时总开关覆盖
2. `ProviderCapability` 最小骨架
3. USD 成本追踪基础
4. `A4` 上下文窗口百分比阈值 + fallback 固定值

#### 阶段 1：可见性与治理最小闭环

1. Method / Skill 列表摘要注入
2. `commander` / `verifier` Agent Profile
3. `ToolExecutor.getDefinitions()` Commander 工具硬拦截

#### 阶段 2：Prompt 精准化与缓存稳定性

1. `A2` 智能固定（Pinning）
2. `A1` 非破坏性 `microcompact` / 前缀缓存稳定性调整
3. Prompt 聚焦 MVP

说明：

- `A2` 与 `A4` 风险最低，优先落地。
- `A1` 必须继续遵守 3.4 中的记忆能力护栏。
- Prompt 聚焦不能重写静态 system prompt，只能走运行时 delta / prelude 注入。

#### 阶段 3：Commander 长任务工作流补全

1. Goal / Node / Run / Agent 隔离键绑定落盘
2. Task Scratch Memory 隔离
3. Review / Lessons Learned 模板
4. 派发、审查、返工、验收闭环
5. `executionMode` / `governanceMode` / `commanderAgentId` / `preferredAgents`

#### 阶段 4：经验资产生产与再合成统一

1. synthesis 模板复用到 `buildMethodDraft()` / `buildSkillDraft()`
2. 已发布资产反序列化为 virtual candidate
3. resynthesize preview / create
4. 最小 UI 或方法管理入口

#### 阶段 5：深度 Token 优化

1. `C1` 更精确的 tokenizer
2. `C3` 迭代预算警告系统
3. `B3` 工具调用修复管线
4. `B4` 缓存对齐摘要路径
5. `B1` 工具结果分级压缩 + 可检索性
6. `B2` 分层模型路由

说明：

- `B1` 与 `B2` 不得前置到阶段 3 之前。
- `B1` 与 `B2` 默认视为高风险后置项；若 3.4 中护栏未满足，应直接 `defer`。
- `B2` 首版不要覆盖 `MEMORY_SUMMARY_*`、`MEMORY_EVOLUTION_*`、`TASK_SUMMARY_*`。

#### 阶段 6：记忆数据库去重

1. `memory.sqlite` 自动备份
2. hash-only exact dedup dry-run
3. hash-only exact dedup apply
4. fuzzy dedup 继续默认关闭，仅保留可选增强开关

### 6.3 并行边界与执行约束

- 阶段 1 与阶段 2 不建议正式并行落地；可并行预研，但不建议并行合并。
- 阶段 4 可以在阶段 1 完成后由独立 owner 预研，但正式落地仍建议晚于阶段 3。
- 阶段 5 中仅 `C1` / `C3` / `B3` 适合较早预研，`B1` / `B2` 不要提前。
- 阶段 6 必须最后处理，不作为抢进度项。
- 任一阶段若会引发记忆能力下降，必须先补护栏、收缩方案或暂缓。

### 6.4 开发进度记录表

以下表格用于开发过程中的进度回写，建议每推进一个阶段或一个关键子项就补一次。

| 记录日期 | 阶段/子项 | 负责人 | 当前状态 | 实际开始 | 预计完成 | 实际完成 | 当前结果 | 风险/阻塞 | 记忆能力影响检查 | 验证情况 | 备注/下一步 |
|----------|-----------|--------|----------|----------|----------|----------|----------|-----------|------------------|----------|-------------|
| 2026-05-17 | 阶段 0 / `BELLDANDY_MEMORY_ENABLED` | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已把记忆总开关接入 Gateway 运行时：关闭总开关时，context injection / auto recall / embedding / summary / evolution / task memory / task summary / deep retrieval 全部随运行时关闭；静态 `MEMORY.md` 注入不受影响 | `@belldandy/core` 构建验证被现有 `src/mcp-config-routing.ts` 空值类型错误阻塞，非本项引入 | 通过 | Unit / Integration | 通过 `memory-runtime-switches.test.ts` 与 Gateway 真实接线完成首轮验证 |
| 2026-05-17 | 阶段 0 / `ProviderCapability` | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已新增主模型能力骨架：支持 `contextWindow` / `cache` / `pricing` / `jsonReliability` 的 env override 解析，并供 Gateway compaction / cost 链路复用 | 当前只覆盖主模型 env override；named fallback / UI 配置暂未扩展 | 通过 | Unit | 已增加 `provider-capability.test.ts`，验证 capability 解析、成本换算与阈值推导 |
| 2026-05-17 | 阶段 0 / USD 成本追踪 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已把 USD 成本字段接入 Agent usage、`token.usage`、自动 `token.counter.result`、conversation meta 持久化链路；在配置主模型价格后可输出 `inputCostUsd` / `outputCostUsd` / `totalCostUsd` | 当前按主模型 env 价格做估算；fallback 模型未做独立定价映射 | 通过 | Unit / Integration | 已验证 token 工具、conversation 持久化、`server.test` 中 `message.send` 自动任务 token 结果链路 |
| 2026-05-17 | 阶段 0 / `A4` 阈值调整 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已实现“上下文窗口百分比 + fallback 固定值”阈值治理：新增 `BELLDANDY_COMPACTION_CONTEXT_WINDOW_FRACTION`，已知主模型 `contextWindow` 时按比例推导，否则继续回退固定阈值 | 若未配置 `BELLDANDY_MODEL_CONTEXT_WINDOW`，行为仍回退为旧固定阈值；这是当前基础版的预期限制 | 通过 | Unit / Integration | 已在 Gateway 主链路接入；helper 测试验证 `128K -> 12.8K` 和 unknown -> fallback 两条路径 |
| 2026-05-17 | 阶段 1 / Method / Skill 摘要注入 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已在 Gateway runtime sections 中新增 Method / Skill 资产摘要注入：按启动时 methods 目录与已加载 skill registry 生成紧凑索引，默认把 method 文件摘要、prompt 已注入 skills、可按需搜索 skills 注入到 system prompt，减少任务启动时重复探测成本 | 资产摘要当前在 Gateway 启动时生成，新增方法/技能后需重启才会刷新；不影响记忆读写链路 | 通过 | Unit / Integration | 已补 `gateway-prompt-sections.test.ts`，并通过 `server.agents.test.ts` / `server.test.ts` 回归 |
| 2026-05-17 | 阶段 1 / Commander / Verifier Profile | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已新增内建 `commander` / `verifier` worker profile：若 `agents.json` 未自定义同名 id，Gateway 启动时自动补齐；`commander` 默认只开放只读/治理/委派工具族，`verifier` 默认沿用 verifier 审查角色与 structured handoff | 现有文档已同步 `docs/agents.json配置说明.md`；若用户已自定义同名 profile，则以用户配置为准 | 通过 | Unit / Integration | 已补 `agent-profile.test.ts`，并通过 `server.test.ts` / `server.agents.test.ts` 回归 |
| 2026-05-17 | 阶段 1 / Commander 硬拦截 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已把 commander 工具硬拦截接入 Gateway `isToolAllowedForAgent`：即使误配白名单，`workspace-write` / `patch` / `command-exec` 家族工具也会在定义阶段直接不可见 | 原先受 `src/mcp-config-routing.ts` 空值类型错误影响的 core build 阻塞已在后续小修复中解除；本项本身无新增阻塞 | 通过 | Unit / Integration | 已新增 `gateway-agent-governance.test.ts`，验证 commander 对写/patch/命令工具的硬阻断优先于白名单 |
| 2026-05-17 | 阶段 2 / `A2` 智能固定 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已在 request compaction 与 `microcompact` 增加高信号 pinning：错误、补丁、关键文件路径、`memory_*` 工具结果优先保留；只调整会话期保留边界，不改长期记忆写入格式 | 当前采用启发式信号匹配；后续若扩展规则需继续约束固定窗口，避免误保留过多无关上下文，但不阻塞 `A1` 启动 | 通过 | Unit / Integration | 已补 `compaction.test.ts` / `microcompact.test.ts` 回归，并顺手修复 `mcp-config-routing.ts` 空值类型构建阻塞 |
| 2026-05-17 | 阶段 2 / `A1` 缓存稳定性 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已给 `microcompact` 增加前缀稳定保护：缓存能力已声明为 supported 的 primary provider 不再原地改写旧 `tool` message，改为保留原始历史并跳过破坏性轻压缩；Gateway 已按 `ProviderCapability.cache` 自动接线 | 当前只覆盖 primary provider 的已声明 cache capability；named fallback / 自定义 fallback 仍未扩展独立 capability 解析，但不阻塞 Prompt 聚焦 MVP 启动 | 通过 | Unit / Integration | 已补 `microcompact.test.ts` / `tool-agent.test.ts`，并通过 `conversation.test.ts`、`server.test.ts` 与相关 build 回归 |
| 2026-05-17 | 阶段 2 / Prompt 聚焦 MVP | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已新增 `prompt-focus-runtime-prelude`：仅对 `AGENTS.md` / `SOUL.md` 做分段与 lexical 聚焦，在 `before_agent_start` 通过 `prependContext` + runtime delta 注入相关片段；静态 `agentWorkspaceCache` 与 `memory.sqlite` 主链路均未改写 | 当前只做 heading/body 词法召回与内存态索引缓存，embedding 增强暂未接入；不影响下一阶段启动 | 通过 | Unit / Integration | 已补 prompt snapshot 级验证，后续若扩展 embedding 仍必须保持独立索引与记忆护栏 |
| 2026-05-17 | 阶段 3 / Run-Node-Goal 绑定 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已新增独立 `goal-runtime-binding-store` 文件账本，并接入 `GoalManager.resume/pause` 与 `SubTaskRuntimeStore` create/attach/complete 链路，统一落盘 `goalId / nodeId / runId / agentId / conversationId / taskId` 绑定 | 当前先覆盖 goal session 与 goal subtask 主链路；bridge / review 等后续场景可继续复用该账本，不影响下一项启动 | 通过 | Unit / Integration | 账本独立于 `memory.sqlite`，后续 Scratch / Review 可直接复用该绑定视图 |
| 2026-05-17 | 阶段 3 / Scratch Memory | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已在 `SubTaskRuntimeStore` 落地文件系统 scratch markdown：goal run 优先写入 `<goal.runtimeRoot>/runs/<runId>/scratch/`，非 goal 子任务回落到 `stateDir/tasks/<taskId>/scratch/`；并已在 `subtask.get` / WebChat 子任务详情页接入 `scratchPath` 与内容展示 | 当前仍采用 task-local / run-local markdown，不依赖 `memory.sqlite`；后续若扩展大体量 scratch 浏览体验，可继续优化 UI，但不阻塞阶段 3 下一项 | 通过 | Unit / Integration / Build | scratch 现已形成“落盘 + 查询 + 前端可见”闭环，可转入 Commander 闭环治理 |
| 2026-05-17 | 阶段 3 / Review / Lessons Learned | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已在 `SubTaskRuntimeStore` 为终态子任务自动生成 review / lesson markdown：goal run 写入 `<goal.runtimeRoot>/runs/<runId>/review-results/review-{taskId}.md`，goal 文档写入 `<goal.docRoot>/lessons-learned/lesson-{taskId}.md`，非 goal 子任务回落到 `stateDir/tasks/<taskId>/...`；`subtask.get` 与 WebChat 子任务详情页已可读取、预览并打开这些产物 | 当前最小实现按 `taskId` 命名避免同 run 多 lane 覆盖；尚未自动做 Commander fan-in 汇总或经验晋升决策，但不阻塞阶段 3 的闭环治理继续推进 | 通过 | Unit / Integration / Manual / Build | 下一步进入阶段 3 / Commander 闭环治理，把 review / lesson 结果接到派发、返工、验收链路 |
| 2026-05-17 | 阶段 3 / Commander 闭环治理 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已把 commander 治理语义接入 capability plan：新增 `governanceMode` / `commanderAgentId` / `preferredAgents`、扩展 `executionMode` 与 `commander_review` fan-in；并在 goal run 下自动生成 `commander-plan.md` 与 `work-order/*.md`，形成派发与收口文件闭环 | 当前仍未引入独立 commander runtime 或自动返工状态机，仍复用现有 `goal_orchestrate` / verifier 编排；这是刻意收缩范围，避免过早放大复杂度 | 通过 | Unit / Integration / Build | 下一步可在此基础上继续补 commander fan-in 汇总、返工决策与经验晋升策略 |
| 2026-05-17 | 阶段 3 / Commander 显式决策工具 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已新增 `goal_commander_decide`，把 commander 决策显式映射到 task node 状态：`accept -> validating`、`rework -> blocked`、`escalate -> validating`；并新增 `require_user_approval` 开关，默认升级到用户最终审批，传 `false` 时允许 Agent 自动收口到 `done` | 当前开关只存在于工具调用层；若后续要上移到 capability plan 或 goal 级默认策略，需要单独评估配置面，但不阻塞阶段 4 启动 | 通过 | Unit / Build | 阶段 3 最小治理闭环已打通；后续若继续深化，优先补审批默认策略与返工 revision 追踪，再考虑阶段 4 资产链路 |
| 2026-05-17 | 阶段 3 / 审批默认策略 + 返工 revision 追踪 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已把 commander 最终审批默认值下沉到 capability plan orchestration：`commander` 节点默认生成 `finalApprovalMode`，低风险且无需 checkpoint 的节点默认 `agent_auto_complete`，其他 commander 节点默认 `user_required`；`goal_commander_decide` 在未显式传参时会跟随该默认值执行，同时支持显式覆盖。已同步新增 `reworkRevisionCount` / `lastReworkReason` / `lastReworkAt`，并写入 `commander-plan.md`、`work-order/*.md`、`review-results/review-*.md` | 当前默认策略已稳定落在 capability plan 层；尚未上移到 goal 级全局设置或 WebChat 审批操作面，但不影响阶段 3 收口完成 | 通过 | Unit / Integration / Build | 阶段 3 当前范围已收口；后续主线建议转入阶段 4，阶段 3 剩余仅作为体验增强项单独排期 |
| 2026-05-17 | 阶段 4 / 模板复用 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已把 goal 侧 method/skill candidate draft 生成收敛到 synthesis 模板规范：新增共享模板解析入口，并让 goal draft 生成优先按 `method-synthesis.md` / `skill-synthesis.md` 的章节结构渲染；模板缺失时仍回退旧硬编码草稿结构 | 当前只完成 4.1，不包含已发布资产反序列化或 resynthesize 放宽；这两个能力仍保持原边界 | 通过 | Unit / Integration / Build | 下一步进入阶段 4.2 / virtual candidate，把已发布 Method / Skill 反序列化为可预览候选 |
| 2026-05-17 | 阶段 4 / virtual candidate | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已新增已发布经验资产桥接：支持从 `stateDir/methods/*.md` 与 `stateDir/skills/*/SKILL.md` 反序列化为只读 virtual candidate；`experience.candidate.synthesize.preview` 已支持 `assetPath` 入口，并把文件系统已发布资产与数据库 draft candidates 一起纳入相似预览 | 当前只解决后端桥接与 preview，本项不负责 create 放宽与 UI 接线；这两部分已分别留给 4.3 与 4.4，不阻塞本项完成 | 通过 | Unit / Integration / Build | virtual candidate 已成为后续 resynthesize 的稳定 seed 形态；同类能力后续应继续优先复用这条桥接链路 |
| 2026-05-17 | 阶段 4 / resynthesize | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已放开 `experience.candidate.synthesize.create` 的 `assetPath` 入口：已发布 Method / Skill 可作为 virtual seed 参与再合成，选中的 draft 与 published sources 会一并进入 LLM 合成，但结果仍只创建新的 draft candidate，不自动覆盖原文件；`markSourcesConsumed` 仅标记真实 draft source，不影响已发布资产 | 当前仍是后端 / RPC 能力，尚未补最小 UI 或方法管理入口；覆盖原文件仍保持人工 accept 语义，避免扩大不可逆风险 | 通过 | Unit / Integration / Build | 阶段 4 的后端主链路已收口；下一步优先做 4.4 最小入口，或转入阶段 5 的低风险项 |
| 2026-05-17 | 阶段 4 / 最小 UI 或方法管理入口 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已把最小入口接到 WebChat `经验能力 -> 能力获取`：在能力获取卡片顶部新增已发布资产再合成输入条，支持 `assetPath -> preview -> create`，并继续复用现有 candidate `accept` 完成最终发布；同时保留原有 draft 泳道的 `合成 / 接受并发布 / 拒绝` 流程不变 | 当前仅完成 WebChat 最小接线，尚未扩展独立方法/技能管理页，也未补更丰富的 published asset 浏览器；但这不阻塞阶段 4 主链路收口 | 通过 | Unit / Integration / Build | 阶段 4 当前定义范围已完成；若继续深化，优先补已发布资产列表化选择与 modal 中的最终 accept 快捷入口 |
| 2026-05-17 | 阶段 4 / 已发布资产列表化 + modal accept 快捷入口 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已在 WebChat `经验能力 -> 能力获取` 页签补齐已发布资产列表化选择，并在 `experienceSynthesisModal` 中新增“接受并发布新草稿”快捷入口；已发布资产可直接从列表进入 preview/create，合成完成后可在 modal 内直接 accept 发布，减少手填 `assetPath` 与页签切换 | 本轮本地页面手测已确认列表入口、填充、preview modal 拉起和 create 请求链路；由于 method/skill synthesis 真实模型调用通常耗时数分钟，本轮未等待到最终 accept 自动切换完成，后续由人工继续做长耗时验收 | 待验证 | Unit / Integration / Manual | 阶段 4 的主链路与体验增强项均已补齐；下一步转入阶段 5 / `C1` tokenizer |
| 2026-05-17 | 阶段 5 / `C1` tokenizer | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已把原 `char/4 + CJK/2` 粗估算升级为独立本地 tokenizer 模块：采用词法分段 + 结构符号 + CJK 感知估算，并预留 `generic/deepseek/openai/anthropic` profile 解析入口；现有 `estimateTokens` / `estimateMessagesTokens` / prompt observability / compaction / 预检链路保持原接口接线 | 当前实现是“本地 tokenizer MVP”，尚未接入外部精确 BPE 词表，也未按具体 provider 返回的原生 usage 反向校准；但已显著优于旧字符启发式，足以支撑阶段 5 的预算管理与预检收口 | 通过 | Unit / Build | 下一步进入阶段 5 / `C3` budget warning；若后续需要更贴近 DeepSeek/OpenAI 原生账单，再单独补精确 BPE 词表或 provider 校准层 |
| 2026-05-17 | 阶段 5 / `C3` budget warning | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已在 `ToolEnabledAgent` 工具循环中新增“模型调用轮次预算”治理：支持 `BELLDANDY_TOOL_LOOP_ITERATION_BUDGET` / `BELLDANDY_TOOL_LOOP_WARNING_FRACTION`，接近预算时向下一轮 prompt 注入收敛 warning delta，超限时先尝试强制 compaction 再明确报错阻断，避免长时间 tool loop 静默烧 token | 当前仅完成 runtime MVP：预算配置已接入 Gateway/env，但尚未补 WebChat 设置面板或更细粒度的 per-profile 覆盖；这是本项刻意收缩范围，不影响阶段 5 下一项启动 | 通过 | Unit / Build | 下一步进入阶段 5 / `B3` 工具调用修复管线；若后续需要运维侧可调体验，再单独补设置 UI 与诊断展示 |
| 2026-05-17 | 阶段 5 / `B3` 工具调用修复 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已在 `ToolEnabledAgent` 落地工具调用修复 MVP：支持按 provider `jsonReliability` 自动启用 repair level，当前覆盖两类高频浪费场景：`tool_call.arguments` 截断 JSON 自动闭合修复，以及连续重复的相同 `tool_name + args` 调用拦截；修复/拦截结果继续复用现有 `tool_result`、failure recovery delta 与 transcript 持久化链路 | 当前只完成 `JSON truncation repair + duplicate suppression` 的轻量闭环，尚未实现完整四遍修复管线、reasoning_content 调用回收或 schema flatten；这是刻意收缩范围，不阻塞阶段 5 下一项启动 | 通过 | Unit / Build | 下一步进入阶段 5 / `B4` 缓存对齐摘要；若后续要继续扩 `B3`，优先补 reasoning scavenging，再评估更激进的 storm/flatten pass |
| 2026-05-17 | 阶段 5 / `B4` 缓存对齐摘要 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已为 compaction summarizer 落地 cache-aligned 摘要 MVP：在 `cache=supported` 的 provider 上，rolling / archival 摘要会向 summarizer 传递结构化上下文，并改为“回放原始摘要素材 + 追加摘要指令 + `<<CACHE_BREAK>>`”的请求构造；非缓存 provider 继续走原有单 prompt 摘要路径，保持兼容 | 当前仅完成摘要请求构造层的 cache alignment，没有补缓存命中率观测、provider 细分策略或独立 diagnostics 展示；这是刻意收缩范围，不阻塞阶段 5 后续项启动 | 通过 | Unit / Build | 下一步进入阶段 5 / `B1` 工具结果分级压缩；若后续要继续深化 `B4`，优先补 cache hit observability，再评估 Anthropic / DeepSeek 的细分策略 |
| 2026-05-17 | 阶段 5 / `B1` 工具结果分级压缩 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已为工具结果压缩补齐独立恢复闭环：`ConversationStore` 新增最近 tool result 持久化与查询接口，`ToolEnabledAgent` 在真实/synthetic `tool_result` 后统一落地摘要与最近结果，新增只读 builtin `retrieve_tool_result` 并接入 Gateway；当前 `microcompact` 仍保持 memory 工具保守策略，不把 `memory_search` / `memory_read` / task memory 工具纳入默认重压缩名单 | 当前恢复链路只覆盖最近 N 条工具结果的 conversation meta 级持久化，尚未扩展到更长期的 transcript/export 或跨会话全文索引；这是刻意收缩范围，不阻塞阶段 5 下一项启动 | 通过 | Unit / Integration / Build | 下一步进入阶段 5 / `B2` 分层模型路由；若后续要压缩 memory 工具结果，必须继续沿用独立恢复入口，不得回退到依赖 `conversation_read` |
| 2026-05-17 | 阶段 5 / `B2` 分层模型路由 | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已为 compaction / 辅助摘要链路补齐分层模型路由 MVP：新增 `resolveCompactionModelRoute(...)`，支持 `primary` / named fallback / `manual:<model>` 与显式 `BELLDANDY_COMPACTION_BASE_URL/API_KEY/MODEL` 覆盖；Gateway 的 compaction summarizer 与 runtime resilience 路由元数据已统一走该解析结果，并新增 `BELLDANDY_COMPACTION_MODEL_ROUTE` 配置入口 | 当前首版刻意只覆盖 compaction / 辅助摘要，不自动扩展到 `MEMORY_SUMMARY_*`、`MEMORY_EVOLUTION_*`、`TASK_SUMMARY_*`；同时因当前摘要请求仍走 OpenAI-compatible body，`anthropic` compaction route 会被显式禁用而不是隐式误发 | 通过 | Unit / Integration / Build | 下一步转入阶段 6 / `dedup dry-run`；若后续继续深化 `B2`，优先分别评估 memory summary/evolution 链路的质量与召回风险，不要直接复制到所有摘要分支 |
| 2026-05-17 | 阶段 6 / dedup dry-run | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已落地 `memory.dedup.preview` 只读预检入口：对 `memory.sqlite` 的 `chunks` 表执行本地规范化内容 hash 分组，输出 exact duplicate group、建议保留项、候选移除项、受影响 source/task link 统计；全程不写库、不删记录、不重建索引 | 当前首版刻意只做 `hash-only exact dedup dry-run`，规范化规则收缩为 `BOM 去除 + CRLF→LF + trim`；不包含自动备份、apply、FTS/向量索引重建，也不做 fuzzy/LLM 判重 | 通过 | Unit / Integration / Build | 下一步进入阶段 6 / `dedup apply` 设计；apply 必须在自动备份、删除候选回放与索引恢复验证补齐后再落地 |
| 2026-05-17 | 阶段 6 / dedup apply | Codex | 已完成 | 2026-05-17 | 2026-05-17 | 2026-05-17 | 已落地 `memory.dedup.apply` 首版：在 dry-run keeper 规则基础上执行 exact duplicate apply，先自动备份 `memory.sqlite`，再事务迁移 `task_memory_links` 到保留 chunk、删除重复 chunk，并在删除后补 `memory_change_seq` 与 FTS rebuild；RPC 侧要求显式 `confirmed=true` 才允许执行；同时已补齐 WebChat `记忆查看 -> 记忆条目` 的 `重复预检 -> 确认清理` 入口与 modal 结果页 | 当前首版仍刻意只处理 `hash-only exact dedup`，不做 fuzzy / LLM 判重；`chunks_vec` 删除与 FTS rebuild 已接线，但本轮没有单独构造 vec-enabled 专项回归。WebChat 现已要求用户在 dry-run 结果 modal 内二次确认，前端会显式调用 `memory.dedup.apply + confirmed=true`，不会自动清理 | 通过 | Unit / Integration / Manual / Build | 阶段 6 的 exact dedup 已形成 `preview -> backup -> apply` 前后端闭环；后续若继续深化，优先补 vec-enabled 专项回归与更细的人工运维说明，不要在当前实现上顺手引入 fuzzy 判重 |

回写要求：

- `当前状态` 只使用：`未开始` / `进行中` / `已完成` / `暂缓`
- `记忆能力影响检查` 必填，凡是不通过或待验证，不得视为可合并完成
- `风险/阻塞` 需要写明是否影响下一阶段启动
- `验证情况` 至少填写本轮实际完成的验证类型与结果
- 若某项改动触发方案收缩、回退或延期，必须在 `备注/下一步` 中记录原因

### 6.5 当前推进记录（阶段 0 首轮）

记录日期：`2026-05-17`

本轮已完成：

1. 落地记忆运行时总开关解析模块，并接入 Gateway 实际运行链路。
2. 落地主模型 `ProviderCapability` 最小骨架，支持 `contextWindow` / `cache` / `pricing` / `jsonReliability` 的 env override。
3. 落地 `A4` 基础版：compaction threshold 改为“上下文窗口百分比 + fallback 固定值”。
4. 落地 USD 成本追踪基础：Agent usage、`token.usage`、`token.counter.result`、conversation meta 已能携带成本字段。

本轮未扩展：

- 未把 `ProviderCapability` 扩展到 named fallback / WebChat 配置面板。
- 未处理 `A1` / `A2` 之外的阶段 2 项。
- 未处理阶段 1 的 Method / Skill 摘要注入与 Commander 治理。

本轮验证：

- 通过 `memory-runtime-switches.test.ts`
- 通过 `provider-capability.test.ts`
- 通过 `packages/belldandy-agent/src/conversation.test.ts`
- 通过 `packages/belldandy-skills/src/builtin/token-counter.test.ts`
- 通过 `packages/belldandy-core/src/server.test.ts -t "message.send emits auto run token result and conversation.meta returns persisted records"`

当前阻塞：

- 当轮记录时，`corepack pnpm --filter @belldandy/core build` 被既有 `packages/belldandy-core/src/mcp-config-routing.ts` 空值类型错误阻塞；该阻塞已在后续阶段 2 / `A2` 首轮中解除。

下一步建议：

1. 继续完成阶段 1：Method / Skill 摘要注入。
2. 随后落 `commander` / `verifier` profile 与工具硬拦截。
3. 在进入阶段 2 前，优先决定是否要把 `ProviderCapability` 继续扩展到 named fallback 配置。

### 6.6 当前推进记录（阶段 1 首轮）

记录日期：`2026-05-17`

本轮已完成：

1. 在 `buildAgentRuntimePromptSections()` 所在链路新增 Method / Skill 资产摘要 section。
2. 启动时从 `stateDir/methods` 与已加载 skill registry 生成 runtime 资产摘要，并按 profile 注入推荐 skill 名称。
3. 新增内建 `commander` / `verifier` worker profile，且支持 `agents.json` 同名覆盖。
4. 为 `commander` 增加 profile 级执行策略提示，明确其职责是编排、委派、收口，而不是直接写代码或跑命令。
5. 把 commander 工具硬拦截接入 Gateway：对白名单误配场景仍强制屏蔽 `workspace-write` / `patch` / `command-exec`。
6. 同步更新 `docs/agents.json配置说明.md`，记录内建 worker profile 与 commander 硬约束行为。

本轮记忆能力影响检查：

- 通过。本轮未修改 `MemoryManager`、召回、摘要、演化、任务记忆或持久化 schema。
- Method / Skill 摘要只增加 runtime prompt 可见性，不替代或压缩记忆内容。
- commander 硬拦截只收紧写工具暴露边界，不影响记忆读取、静态 `MEMORY.md` 注入或已有记忆写入路径。

本轮验证：

- 通过 `packages/belldandy-agent/src/agent-profile.test.ts`
- 通过 `packages/belldandy-core/src/bin/gateway-prompt-sections.test.ts`
- 通过 `packages/belldandy-core/src/bin/gateway-agent-governance.test.ts`
- 通过 `packages/belldandy-core/src/server.agents.test.ts`
- 通过 `packages/belldandy-core/src/server.test.ts`
- 当轮记录时，`corepack pnpm --filter @belldandy/agent --filter @belldandy/skills --filter @belldandy/core build` 仍被既有 `packages/belldandy-core/src/mcp-config-routing.ts` 空值类型错误阻塞，`@belldandy/agent` 与 `@belldandy/skills` build 已通过；该阻塞已在后续阶段 2 / `A2` 首轮中解除

当前阻塞：

- 当轮唯一阻塞是 `packages/belldandy-core/src/mcp-config-routing.ts` 的空值类型错误；该阻塞已在后续阶段 2 / `A2` 首轮中修复。

下一步建议：

1. 阶段 2 先做 `A2` 智能固定（Pinning）。
2. 随后推进 `A1` 非破坏性 `microcompact` / 前缀缓存稳定性调整。
3. `mcp-config-routing.ts` 的既有空值类型问题已在后续小任务中修复，后续可直接进入 `A1`。

---

### 6.7 当前推进记录（阶段 2 / A2 首轮）

记录日期：`2026-05-17`

本轮已完成：

1. 新增 `pinning.ts`，用启发式规则识别高信号内容：关键文件路径、错误/异常、补丁/变更。
2. 在 `compactIncremental()` 中把 `A2` 收敛为“扩大连续尾部保留窗口”，当最近消息命中高信号时，同时保留该消息与紧邻配对消息，不把它们提前送入摘要。
3. 在 `microcompactMessages()` 中优先压缩普通旧工具输出，保留最近高信号工具结果与 `memory_*` 工具结果，避免在会话内过早抹掉关键排障/记忆读取内容。
4. 修复 `packages/belldandy-core/src/mcp-config-routing.ts` 中 `local` / `central` 的空值类型判断，恢复 `@belldandy/core` 相关 build 绿灯。

本轮记忆能力影响检查：

- 通过。本轮没有修改 `MemoryManager`、durable memory schema、`memory_search` / `memory_read` 接口、session memory 摘要写入格式或召回链路。
- `A2` 只改变会话期 compaction / microcompact 的保留边界，不把“固定”做成长期记忆结构改写。
- 对 `memory_*` 工具结果的保留仅发生在最近窗口内，目的是避免会话内记忆读取结果先被普通工具输出挤掉，不会阻断后续记忆写入或读取。

本轮验证：

- 通过 `node .\node_modules\vitest\vitest.mjs run packages/belldandy-agent/src/compaction.test.ts packages/belldandy-agent/src/microcompact.test.ts packages/belldandy-agent/src/conversation.test.ts packages/belldandy-core/src/server.test.ts --reporter verbose`
- 通过 `node .\node_modules\vitest\vitest.mjs run packages/belldandy-core/src/mcp-config-routing.test.ts packages/belldandy-core/src/cli/commands/doctor.test.ts packages/belldandy-core/src/server.doctor.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/agent --filter @belldandy/skills --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- `mcp-config-routing.ts` 的空值类型构建阻塞已解除。

下一步建议：

1. 进入阶段 2 / `A1`：继续做非破坏性 `microcompact` / 前缀缓存稳定性调整。
2. `A1` 若需要扩大压缩或缓存策略范围，必须继续保持“只动会话期边界、不降低记忆能力”的护栏。
3. 若后续发现 pinning 命中过宽，应优先缩小启发式规则或 lookback 窗口，而不是放松记忆相关保护。

---

### 6.8 当前推进记录（阶段 2 / A1 首轮）

记录日期：`2026-05-17`

本轮已完成：

1. 给 `microcompactMessages()` 增加 `preservePrefixStability` 保护开关，并在命中可压缩旧工具结果时返回“因前缀稳定策略跳过 destructive rewrite”的观测结果。
2. 保持 `A2` 的高信号 pinning 逻辑不变，仅收缩 `A1` 到“禁止原地改写旧 tool message”这一最小闭环，不改 session transcript、compaction state 或长期记忆格式。
3. 在 `ToolEnabledAgent` 中补充前缀稳定跳过日志，避免把缓存敏感路径上的 `microcompact` 静默吞掉。
4. 在 Gateway 中基于 `ProviderCapability.cache` 为 primary provider 自动启用前缀稳定保护；当主模型声明 `cache=supported` 时，默认停用破坏性 `microcompact` 改写。

本轮记忆能力影响检查：

- 通过。本轮没有修改 `MemoryManager`、durable extraction、`memory_search` / `memory_read`、session memory 摘要结构或记忆写入 schema。
- 旧工具结果在缓存敏感路径上改为“保留原文，不做原地清空”，这只减少破坏性压缩，不会削弱记忆读取或最近工作上下文。
- 会话级 compaction、`MEMORY.md` 静态注入、`<recent-memory>` / `<auto-recall>` 动态注入与 task memory hook 均未改动，相关回归已通过。

本轮验证：

- 通过 `node .\node_modules\vitest\vitest.mjs run packages/belldandy-agent/src/microcompact.test.ts packages/belldandy-agent/src/tool-agent.test.ts --reporter verbose`
- 通过 `node .\node_modules\vitest\vitest.mjs run packages/belldandy-agent/src/conversation.test.ts packages/belldandy-core/src/server.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/agent --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 当前实现只覆盖 primary provider 的已声明 cache capability；fallback provider 仍未接入独立 capability 解析，这是已知范围收缩，不影响下一步启动。

下一步建议：

1. 进入阶段 2 / Prompt 聚焦 MVP。
2. Prompt 聚焦继续严格走运行时 delta / prelude 注入，不改写静态 system prompt。
3. 若后续要把 `A1` 扩展到 fallback provider，优先补 named fallback capability 解析，不要回退到按 provider 名称猜测缓存能力。

---

### 6.9 当前推进记录（阶段 2 / Prompt 聚焦 MVP）

记录日期：`2026-05-17`

本轮已完成：

1. 新增 `packages/belldandy-core/src/prompt-focus-runtime-prelude.ts`，仅针对 `AGENTS.md` / `SOUL.md` 构建独立的运行时 lexical index：按 Markdown 标题分段、长段落切块、按当前用户输入做 heading/body 词法打分。
2. 在 `before_agent_start` 链路接入 Prompt 聚焦 hook，保持注入形式为 `prependContext` + `user-prelude` delta，只在当前 run 动态生效，不改写 `agentWorkspaceCache` 中的静态 system prompt。
3. 给 Gateway 增加 `agentId -> workspaceDir` 绑定解析，确保非 default agent 也能命中各自 workspace 下的 `AGENTS.md` / `SOUL.md`。
4. 补充 `prompt-focus-runtime-prelude.test.ts`，覆盖分段、词法召回、无命中跳过、prompt snapshot 保持静态 system prompt 不变等关键行为。

本轮记忆能力影响检查：

- 通过。本轮没有修改 `MemoryManager`、`memory.sqlite`、`memory_search` / `memory_read`、durable memory 写入格式、summary/evolution/task memory 开关或召回链路。
- Prompt 聚焦索引独立于长期记忆主链路，只在运行时基于 workspace 文档构建内存态 lexical index，不参与普通记忆去重、演化或召回共池。
- 注入位置是 `before_agent_start` 的易变 prelude/delta，不会替换静态 `AGENTS.md` / `SOUL.md` system section，也不会压缩或覆盖现有记忆相关提示。

本轮验证：

- 通过 `node .\node_modules\vitest\vitest.mjs run packages/belldandy-core/src/prompt-focus-runtime-prelude.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/core --filter @belldandy/agent build`

当前阻塞：

- 无新增阻塞。
- 当前范围收缩为 lexical / heading-body 召回 + 内存态索引缓存；embedding 增强与持久化索引未纳入本轮 MVP，但不影响后续阶段启动。

下一步建议：

1. 若继续推进阶段 3，可先从 Run-Node-Goal 绑定与 Scratch Memory 开始，保持“任务期痕迹不进长期记忆库”的边界。
2. 若要增强 Prompt 聚焦命中率，优先加可选 embedding 辅助，但必须继续与 `memory.sqlite` 主召回链路隔离。
3. 在真实 DeepSeek / cache 路线下补一轮 Manual 观察，确认 prompt snapshot 与缓存 usage 字段没有因动态聚焦出现异常波动。

### 6.10 当前推进记录（阶段 3 / Scratch Memory）

记录日期：`2026-05-17`

本轮已完成：

1. 在 `packages/belldandy-core/src/task-runtime.ts` 为 `SubTaskRecord` 增加 `scratchPath`，并在 persist 阶段同步生成 scratch markdown 文件。
2. 已实现 goal run 与普通子任务双路径落盘：
   - goal run 走 `<goal.runtimeRoot>/runs/<runId>/scratch/scratch-{agentId}.md`
   - 非 goal 子任务走 `stateDir/tasks/<taskId>/scratch/scratch-{agentId}.md`
3. 已把 thought delta 作为 `progress` 通知纳入 scratch 汇总，避免子任务完成后被最终 `Task completed.` 覆盖，确保中间假设/排查观察仍可回看。
4. 已在 `packages/belldandy-core/src/task-runtime.test.ts` 补充两类回归：
   - goal run scratch 落盘
   - 非 goal fallback scratch 落盘
5. 已同步把 `scratchPath` 暴露到 `packages/belldandy-skills/src/types.ts` 的 `SessionInfo`，为后续 runtime 查询或 UI 展示预留字段。

本轮记忆能力影响检查：

- 通过。scratch 仅写入文件系统 markdown，不进入 `memory.sqlite`、durable extraction、memory summary、memory evolution 或 task memory 主链路。
- goal run 路径解析依赖已有 `goalId/nodeId/runId` 会话语义与 goal registry，不新增长期记忆 schema 或召回耦合。
- thought delta 的保留仅发生在 subtask runtime 的本地 scratch 汇总，不会改变长期记忆写入格式。

本轮验证：

- 通过 `node .\node_modules\vitest\vitest.mjs run packages/belldandy-core/src/task-runtime.test.ts packages/belldandy-core/src/goal-runtime-binding-store.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/core build`

当前阻塞：

- 无阻塞当前阶段继续推进的新增问题。
- `scratchPath` 目前已在 runtime/type 层暴露，但尚未接入 WebChat 或 review 展示链路；这是后续体验增强项，不阻塞阶段 3 下一子项启动。

下一步建议：

1. 进入阶段 3 / Review / Lessons Learned，优先复用现有 goal runtime 目录与绑定账本，保持“任务期痕迹不进长期记忆库”的边界。
2. 若后续需要前端可见性，可在 subtask detail / query runtime 视图补充 `scratchPath` 展示或预览入口。
3. 若后续发现 scratch 写盘频率偏高，优先收紧汇总窗口或持久化节流，不要回退到放弃 thought delta 保留。

### 6.11 当前推进记录（阶段 3 / Review / Lessons Learned）

记录日期：`2026-05-17`

本轮已完成：

1. 在 `packages/belldandy-core/src/task-runtime.ts` 为 `SubTaskRecord` 增加 `reviewPath` / `lessonPath`，并在子任务进入终态或归档时自动生成 review / lesson markdown。
2. 已实现 goal run 与普通子任务双路径落盘：
   - review 走 `<goal.runtimeRoot>/runs/<runId>/review-results/review-{taskId}.md`
   - lesson 走 `<goal.docRoot>/lessons-learned/lesson-{taskId}.md`
   - 非 goal 子任务统一回落到 `stateDir/tasks/<taskId>/review-results/` 与 `stateDir/tasks/<taskId>/lessons-learned/`
3. 已在 `packages/belldandy-core/src/query-runtime-subtask.ts` 扩展 `subtask.get`，返回 `scratchContent`、`reviewContent`、`lessonContent`，让运行时查询可直接消费这些 markdown 产物。
4. 已在 `apps/web/public/app/features/subtasks-overview.js` 复用现有详情页，新增 Scratch / Commander Review / Lessons Learned 三类 artifacts 的预览与打开入口，不新增顶层导航。
5. 已补充：
   - `packages/belldandy-core/src/task-runtime.test.ts`
   - `packages/belldandy-core/src/server.query-runtime-domains.test.ts`
   - `apps/web/public/app/features/subtasks-overview.test.js`

本轮记忆能力影响检查：

- 通过。review / lesson 与 scratch 一样仅写文件系统 markdown，不进入 `memory.sqlite`、durable extraction、memory summary、memory evolution 或 task memory 主链路。
- review / lesson 路径解析只复用已有 `goalId / nodeId / runId / agentId` 绑定账本与 goal registry，不新增长期记忆 schema 或召回耦合。
- WebChat 详情页只读取 runtime artifact 内容，不把这些文档自动晋升为长期记忆；是否沉淀为经验仍留给后续 Commander / 经验链路决策。

本轮验证：

- 通过 `node .\node_modules\vitest\vitest.mjs run packages/belldandy-core/src/task-runtime.test.ts packages/belldandy-core/src/server.query-runtime-domains.test.ts apps/web/public/app/features/subtasks-overview.test.js --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 当前实现仍是 task 级 review / lesson 产物，不包含 Commander fan-in 总结或自动经验晋升；这是后续阶段 3 / Commander 闭环治理要继续补的内容，但不影响本子项完成。

下一步建议：

1. 进入阶段 3 / Commander 闭环治理，把 review / lesson 结果与派发、返工、验收状态串成完整 fan-in 工作流。
2. 若后续需要 run 级单文件总复盘，可在不破坏当前 task 级唯一命名的前提下，再补 Commander 汇总文档，而不是回退到覆盖式文件名。
3. 若后续要把 lessons learned 晋升到经验资产或长期记忆，必须继续保留人工确认或 Commander 审查护栏。

### 6.12 当前推进记录（阶段 3 / Commander 闭环治理）

记录日期：`2026-05-17`

本轮已完成：

1. 在 `packages/belldandy-core/src/goals/types.ts` 与 `packages/belldandy-skills/src/types.ts` 扩展 capability plan 契约：
   - 新增 `governanceMode`
   - 新增 `commanderAgentId`
   - 新增 `preferredAgents`
   - `executionMode` 扩展为 `single_agent` / `multi_agent` / `multi_agent_parallel` / `multi_agent_sequential` / `auto`
   - `fanInStrategy` 扩展为 `commander_review`
   - `coordinationPlan` 新增 `managerAgentId`
2. 在 `packages/belldandy-core/src/goals/capability-planner.ts` 接入 commander 治理启发式：
   - 多分工、非低风险或显式多代理模式时，自动优先 `commander`
   - 单点、低风险节点保持 `direct`
   - 继续复用现有 `method` / `skill` / `mcp` / agent catalog 规划链路，不单独引入新 runtime
3. 在 `packages/belldandy-skills/src/builtin/goals/goal-orchestrate.ts` 与 `shared.ts` 对齐治理语义：
   - `inferRolePolicy()` / `getCoordinationPlan()` 可识别 `commander_review`
   - 展示层可直接看到 governance、commander、preferredAgents 与 manager 信息
   - 为避免扩大协议面，底层 sub-agent `aggregationMode` 仍保持现有 `main_agent_summary` / `verifier_fan_in`
4. 在 `packages/belldandy-core/src/goals/manager.ts` 为 commander 模式自动落盘：
   - `<goal.runtimeRoot>/runs/<runId or node fallback>/commander-plan.md`
   - `<goal.runtimeRoot>/runs/<runId or node fallback>/work-order/<agent>.md`
   这些文件会记录目标、分工、交付物、out-of-scope 与 verification hints，形成最小 work order 闭环。
5. 已补测试：
   - `packages/belldandy-core/src/goals/capability-planner.test.ts`
   - `packages/belldandy-core/src/goals/manager.test.ts`
   - `packages/belldandy-skills/src/builtin/goals/capability-plan-utils.test.ts`

本轮记忆能力影响检查：

- 通过。新增 commander 计划与 work order 仅写入 goal runtime 文件系统，不触碰 `memory.sqlite`、embedding、durable memory、task memory schema 或召回主链路。
- 治理字段只挂在 capability plan runtime state 与 UI/编排契约层，不把任务期治理痕迹混入长期记忆对象。
- commander 闭环目前仍是“运行时文件治理 + 已有编排能力复用”，没有引入自动长期记忆晋升，因此不存在额外记忆退化风险。

本轮验证：

- 通过 `node .\node_modules\vitest\vitest.mjs run packages/belldandy-core/src/goals/capability-planner.test.ts packages/belldandy-core/src/goals/manager.test.ts packages/belldandy-skills/src/builtin/goals/capability-plan-utils.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/core --filter @belldandy/skills build`

当前阻塞：

- 无新增阻塞。
- 当前实现刻意未引入独立 commander agent runtime、自动返工循环或 fan-in 汇总状态机；这属于后续增强项，不影响本轮阶段 3 闭环治理最小交付。

下一步建议：

1. 若继续推进阶段 3 深化，可在现有 `commander-plan.md` / `work-order/*.md` 之上补 `review-results` 汇总与返工状态迁移，而不是另起一套治理存储。
2. 若后续需要真正的 commander 自动复审/返工流程，优先复用现有 `goal_orchestrate` 与 verifier handoff 结构，再决定是否值得新增独立状态机。
3. 若后续要把 commander review / lessons learned 晋升为经验资产，必须保留人工确认或明确的 governance gate，继续守住“任务期痕迹不直接进长期记忆”的边界。

### 6.13 当前推进记录（阶段 3 / Commander fan-in 汇总与验收门）

记录日期：`2026-05-17`

本轮已完成：

1. 在 `packages/belldandy-core/src/goals/capability-acceptance-gate.ts` 为 `commander_review` 新增 acceptance gate 分支：
   - 所有 delegated lane 成功且无 pending / failed 时记为 `accepted`
   - 任一路失败时记为 `rejected`
   - 仍有待回传 lane 时记为 `pending`
   - 同时产出 `managerActionHint`，明确是等待、返工还是可验收收口
2. 在 `packages/belldandy-core/src/goals/manager.ts` 为 commander 模式补 run 级 review 汇总：
   - `<goal.runtimeRoot>/runs/<runId or node fallback>/review-results/review-<node>.md`
   - 内容包含 delegation results、acceptance checks、next action 和 manager hint
3. 在 `packages/belldandy-skills/src/builtin/goals/shared.ts` 补充 `Acceptance Gate` 展示，`goal_orchestrate` 输出现在可直接看到 commander fan-in 是否已验收通过。
4. 在 `packages/belldandy-skills/src/builtin/goals/goal-orchestrate.ts` 修正多代理模式识别，`multi_agent_parallel` / `multi_agent_sequential` 不再被误判成非多代理路径。
5. 已补测试：
   - `packages/belldandy-core/src/goals/capability-acceptance-gate.test.ts`
   - `packages/belldandy-core/src/goals/manager.test.ts`
   - `packages/belldandy-skills/src/builtin/goals/goal-tools.test.ts`

本轮记忆能力影响检查：

- 通过。新增 acceptance gate 与 commander review 汇总仍只作用于 goal runtime 文件和 capability plan runtime state。
- 未引入新的 `memory.sqlite` 表、字段或 durable memory 写入链路。
- 返工/验收结论仍停留在任务期治理层，是否晋升为长期经验仍需后续显式决策。

本轮验证：

- 通过 `node .\node_modules\vitest\vitest.mjs run packages/belldandy-core/src/goals/capability-acceptance-gate.test.ts packages/belldandy-core/src/goals/manager.test.ts packages/belldandy-skills/src/builtin/goals/goal-tools.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/core --filter @belldandy/skills build`

当前阻塞：

- 无新增阻塞。
- 当前仍未自动驱动 task node 进入 `validating` / `done` / `blocked`，只提供明确的 fan-in 验收门与 review 汇总；这是刻意保留的人控边界。

下一步建议：

1. 若继续推进阶段 3，可基于当前 `Acceptance Gate` 结果补一个显式 commander 决策工具，把“接受 / 返工 / 升级”映射到 task node 状态迁移。
2. 若要继续增强返工闭环，优先复用现有 `work-order/*.md` 与 `review-results/*.md`，补 revision count / rework reason，而不是新开治理存储。

### 6.14 当前推进记录（阶段 3 / Commander 显式决策工具）

记录日期：`2026-05-17`

本轮已完成：

1. 在 `packages/belldandy-skills/src/builtin/goals/goal-commander-decide.ts` 新增 `goal_commander_decide` 工具，专门面向 `governanceMode=commander` 的 capability plan。
2. 工具会先读取当前节点的 capability plan 与 `acceptanceGate`，并要求：
   - commander 模式下才允许执行
   - `decision=accept` 时必须先满足 `acceptanceGate.status=accepted`
3. 本轮按显式最小映射落地，并给 `escalate` 增加最终审批开关：
   - `accept -> validating`
   - `rework -> blocked`
   - `escalate -> validating`（默认，等待用户最终审批）
   - `escalate + require_user_approval=false -> done`（Agent 自动收口）
4. 工具会把 commander 决策摘要以 note 形式追加到 `orchestration.notes`，继续复用既有 capability plan runtime state，不新增治理存储。
5. 已同步接入：
   - `packages/belldandy-skills/src/builtin/goals/index.ts`
   - `packages/belldandy-skills/src/index.ts`
6. 已在 `packages/belldandy-skills/src/builtin/goals/goal-tools.test.ts` 补充显式决策测试，覆盖：
   - accept -> validating
   - rework -> blocked
   - escalate 默认进入 validating
   - 关闭 `require_user_approval` 后 escalate -> done

本轮记忆能力影响检查：

- 通过。新增 commander 决策工具只读写 capability plan runtime state 与 task node runtime 状态，不触碰 `memory.sqlite`、durable memory、memory summary、memory evolution 或 task memory schema。
- commander 决策备注仅追加到 `orchestration.notes` 与既有 runtime artifact 闭环中，不把任务期治理决策直接晋升为长期记忆。

本轮验证：

- 通过：
  - `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-skills/src/builtin/goals/goal-tools.test.ts --reporter verbose`
  - `corepack pnpm --filter @belldandy/skills --filter @belldandy/core build`

当前阻塞：

- 无设计级新增阻塞。
- 当前 `escalate` 已改为“默认升级到用户最终决策 / 进入 validating”，并通过 `require_user_approval` 开关保留 Agent 自动收口路径；若后续还要把该开关上移到 capability plan 或 goal 级默认策略，应单独评估配置面，不要直接扩大本轮工具语义。

---

### 6.15 后续可推进计划（建议顺序）

记录日期：`2026-05-17`

当前判断：

- 阶段 3 的最小 commander 治理闭环已完成，当前没有阻塞阶段 4 启动的关键缺口。
- 若继续深挖阶段 3，建议只补“默认策略”和“返工追踪”两类低扩散项；不要在这一轮引入独立 commander runtime 或复杂自动状态机。

建议推进顺序：

1. 阶段 3.1 / Commander 审批默认策略
   - Goal：把 `require_user_approval` 从工具调用参数上移到 capability plan 或 goal 级默认配置，减少每次手动传参。
   - Effect：让“哪些节点必须用户最终验收、哪些节点允许 Agent 自动收口”变成稳定治理规则，而不是调用时约定。
   - Risk：中。会触及规划契约与默认值传播，但不需要新增存储引擎。
   - Boundary：只补默认策略读取与展示，不在本项引入新的审批状态机。

2. 阶段 3.2 / 返工 revision 追踪
   - Goal：在既有 `work-order/*.md`、`review-results/*.md` 与 capability plan notes 上补 revision count / rework reason。
   - Effect：让 commander 的“第几轮返工、为什么返工、是否已消除上轮问题”可追踪。
   - Risk：低到中。主要是 runtime artifact 结构扩展，不触碰长期记忆。
   - Boundary：只做 runtime 文件与状态补充，不做自动返工循环。

3. 阶段 3.3 / WebChat 最终审批体验
   - Goal：把 `goal_commander_decide` 的 accept / rework / escalate 操作接入现有子任务或 goal 详情视图，减少只能靠工具调用的摩擦。
   - Effect：用户能直接在界面完成最终审批或切换到 Agent 自动收口模式。
   - Risk：中。涉及前端动作接线和状态回显，但不改变治理模型。
   - Boundary：复用现有详情页，不新增顶层导航。

4. 阶段 4 / 经验资产生产与再合成统一
   - Goal：继续按既定路线推进模板复用、virtual candidate、resynthesize。
   - Effect：统一 Method / Skill 草稿、发布与再合成口径，解决资产链路双轨漂移。
   - Risk：中。会改经验资产输出口径，但不应回灌到 commander 运行时治理层。
   - Boundary：先做模板复用，再做 virtual candidate，最后做 resynthesize；不要反过来推进。

推荐结论：

- 若你要继续强化“Commander 真的可用”，优先做 `阶段 3.1 + 3.2`。
- 若你要尽快切到下一主线，当前可以直接进入阶段 4 / 模板复用。

### 6.16 当前推进记录（阶段 3 / 审批默认策略 + 返工 revision 追踪）

记录日期：`2026-05-17`

本轮已完成：

1. 在 capability plan orchestration 契约中新增：
   - `finalApprovalMode`
   - `reworkRevisionCount`
   - `lastReworkReason`
   - `lastReworkAt`
2. 在 `packages/belldandy-core/src/goals/capability-planner.ts` 为 commander 节点补默认审批策略：
   - 低风险且不要求 checkpoint：默认 `agent_auto_complete`
   - 其他 commander 节点：默认 `user_required`
3. 在 `packages/belldandy-core/src/goals/manager.ts` 增加兜底默认值：即使 commander plan 不是从标准 planner 路径生成，`saveCapabilityPlan()` 也会自动补齐 `finalApprovalMode`。
4. 在 `packages/belldandy-skills/src/builtin/goals/goal-commander-decide.ts` 改为：
   - 未显式传 `require_user_approval` 时，跟随 plan 的 `finalApprovalMode`
   - 显式传参时，覆盖 plan 默认值
   - `rework` 决策会自动累计 `reworkRevisionCount` 并记录最后返工原因/时间
5. 在 commander artifacts 展示层补充审批与返工信息：
   - `commander-plan.md`
   - `work-order/*.md`
   - `review-results/review-*.md`
   - `formatCapabilityPlan()` 输出

本轮记忆能力影响检查：

- 通过。新增字段仍只保存在 capability plan runtime state 与 commander runtime artifacts 中，不触碰 `memory.sqlite`、durable memory、memory summary、memory evolution 或 task memory schema。
- 返工 revision 与审批默认策略均停留在任务期治理层，不会自动晋升为长期记忆或经验资产。

本轮验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-core/src/goals/capability-planner.test.ts packages/belldandy-core/src/goals/manager.test.ts packages/belldandy-skills/src/builtin/goals/capability-plan-utils.test.ts packages/belldandy-skills/src/builtin/goals/goal-tools.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/skills --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 阶段 3 当前定义范围内的治理闭环、审批默认策略与返工 revision 追踪已完成；剩余项主要是体验增强，不再阻塞阶段 4 主线。

下一步建议：

1. 正式切入阶段 4 / 模板复用，先统一 method/skill draft 生成与 synthesis 模板口径。
2. 阶段 4 稳定后，再进入 `virtual candidate -> resynthesize`，不要跳过模板复用直接做已发布资产再合成。
3. 阶段 3 若还有后续，只建议作为增强项单独排期：
   - WebChat 最终审批入口
   - goal 级默认审批设置
   - commander 返工轮次可视化

### 6.17 当前推进记录（阶段 4 / virtual candidate + resynthesize）

记录日期：`2026-05-17`

本轮已完成：

1. 已完成阶段 4.2 / `virtual candidate`：
   - `experience.candidate.synthesize.preview` 新增 `assetPath` 入口。
   - 已发布 `Method / Skill` 可从 `stateDir/methods/*.md` 与 `stateDir/skills/*/SKILL.md` 反序列化为 virtual candidate。
   - virtual candidate 已可与数据库中的 draft candidates 一起参与相似预览。
2. 已完成阶段 4.3 / `resynthesize`：
   - `experience.candidate.synthesize.create` 已支持 `assetPath` 入口。
   - 已发布资产可作为 virtual seed 再次合成，选中的 draft / published sources 会一起进入 synthesis prompt。
   - 合成结果仍只创建新的 draft candidate，不直接覆盖已发布文件。
   - `markSourcesConsumed` 仅消耗真实 draft source，不会把文件系统已发布资产错误标记为 consumed。

关键验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-core/src/server.memory-experience.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/memory --filter @belldandy/core build`

当前阻塞：

- 无新增主链路阻塞。
- 阶段 4 的后端与 WebChat 最小入口都已打通；当前剩余仅是体验增强，不再阻塞阶段 5 启动。

下一步建议：

1. 若继续留在阶段 4，优先补“已发布资产列表化选择”和 modal 内的最终 `accept` 快捷入口，减少手填 `assetPath` 和页签切换成本。
2. 若转主线，建议直接进入阶段 5 的低风险项：`C1 tokenizer -> C3 budget warning -> B3 工具调用修复管线`。
3. 阶段 6 / dedup 继续保持最后处理，不要为了清理数据提前打乱阶段顺序。

### 6.18 当前推进记录（阶段 4 / 最小 UI 或方法管理入口）

记录日期：`2026-05-17`

本轮已完成：

1. 已在 WebChat `经验能力 -> 能力获取` 页签补最小入口：
   - 保留原有 `Method Draft / Skill Draft` 双泳道。
   - 在能力获取卡片顶部新增已发布资产再合成输入条。
   - 支持直接输入 `stateDir/methods/*.md` 或 `stateDir/skills/*/SKILL.md` 对应 `assetPath` 发起 preview。
2. 已把 `assetPath -> preview -> create` 串到现有 `experienceSynthesisModal`：
   - preview 支持 published asset virtual seed。
   - create 支持 published asset 再合成生成新的 draft candidate。
   - 最终发布仍复用现有 candidate `accept`，保持人工验收语义。
3. 已调整能力获取页签空态：
   - 即使当前没有 draft candidates，已发布资产再合成输入条仍可使用。

关键验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run apps/web/public/app/features/experience-workbench.test.js --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/memory --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 当前仅缺更强的体验增强项，不影响阶段 4 收口完成。

### 6.19 当前推进记录（阶段 5 / C1 tokenizer）

记录日期：`2026-05-17`

本轮已完成：

1. 已把原先散落在 `compaction.ts` 中的 `char/4 + CJK/2` 粗略估算抽离为独立本地 tokenizer 模块：
   - 新增 `packages/belldandy-agent/src/tokenizer.ts`
   - 采用词法分段 + 结构符号 + CJK 感知估算，替代单纯按字符长度折算
   - 预留 `generic / deepseek / openai / anthropic` profile 解析入口，为后续 provider 校准留接缝
2. 已保持现有主链路接口兼容：
   - `estimateTokens` 继续由 `@belldandy/agent` 暴露
   - `compaction`、prompt 观测、消息预检等现有调用点无需改签名即可使用新估算器
3. 已补充阶段 5 / `C1` 的首轮单元验证：
   - 英文短句估算更贴近自然词边界
   - 结构化代码/JSON 文本估算高于普通 prose
   - CJK 文本估算高于旧字符启发式，减少“预检看起来够、实际调用超限”的低估风险

关键验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-agent/src/tokenizer.test.ts packages/belldandy-agent/src/compaction.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/agent --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 当前 `C1` 已形成可用 MVP；若要继续提升到账单级精度，需要后续再补精确 BPE 词表或 provider usage 校准，但这不阻塞 `C3` 启动。

下一步建议：

1. 正式进入阶段 5 / `C3`：在工具调用循环中补迭代预算警告与强制摘要闸门。
2. `B3` 工具调用修复管线继续保持在 `C3` 之后推进，不要与预算警告混做一项。
3. 若后续要强化 `C1`，优先做 provider 校准层，不要先把 `B1` / `B2` 高风险项提前。

### 6.20 当前推进记录（阶段 5 / C3 budget warning）

记录日期：`2026-05-17`

本轮已完成：

1. 已在 `packages/belldandy-agent/src/tool-agent.ts` 的主循环中新增“模型调用轮次预算”治理：
   - 新增 `toolLoopIterationBudget` / `toolLoopWarningFraction` 运行时选项
   - 默认预算为 `64` 轮，warning 阈值默认 `0.7`
   - 预算接近时通过 `pendingToolFollowupDeltas` 向下一轮 prompt 注入一次性 warning delta，提示模型尽快收敛、总结或减少工具调用
2. 已在预算超限时增加阻断前的强制 compaction 尝试：
   - 复用现有 `compactInLoop()` / `compactIncremental()` 链路
   - 进入 `budget + 1` 轮前，先尝试 `force` 压缩，再返回明确 error，避免继续空转调用模型
3. 已补齐运行时配置接线：
   - `packages/belldandy-core/src/bin/gateway.ts` 已解析并透传 `BELLDANDY_TOOL_LOOP_ITERATION_BUDGET`
   - `packages/belldandy-core/src/bin/gateway.ts` 已解析并透传 `BELLDANDY_TOOL_LOOP_WARNING_FRACTION`
   - `config-channel` 白名单与 `runtime.env` 模板已同步更新
4. 已补充阶段 5 / `C3` 的首轮回归测试：
   - 接近预算时会记录 warning，并把 `Iteration Budget Warning` 注入下一轮 prompt snapshot
   - 超限时会在第二轮模型调用前直接阻断，并触发一次强制 compaction 路径

本轮记忆能力影响检查：

- 通过。本轮没有修改 `MemoryManager`、`memory_search` / `memory_read`、durable extraction、memory summary / evolution、task memory 或任何长期记忆 schema。
- 新增治理只作用于 `ToolEnabledAgent` 的单次 run 工具循环预算，不改静态 `MEMORY.md` 注入，也不削弱 `before_agent_start` 的 `<recent-memory>` / `<auto-recall>` 注入链路。
- 强制 compaction 仅复用既有会话期压缩路径，不把预算阻断信息写入长期记忆，也不把 scratch / tool loop 控制信息混入 memory 数据库。

关键验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-agent/src/tool-agent.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/agent --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 当前实现是 runtime MVP，未扩展 WebChat 设置面板、diagnostics 展示或 per-profile 覆盖；这些属于后续体验增强，不阻塞 `B3` 启动。

下一步建议：

1. 正式进入阶段 5 / `B3` 工具调用修复管线，继续保持“最小闭环、不要混入 `B1` / `B2`”的边界。
2. 若后续需要更强的运维可见性，可单独补充 budget warning 的 diagnostics / settings 展示，但不要与 `B3` 主线混做。
3. `maxToolCalls` 现有语义已保持不变；后续若要继续细化预算治理，优先做 per-profile / per-task override，而不是扩大默认阻断强度。

### 6.21 当前推进记录（阶段 5 / B3 工具调用修复）

记录日期：`2026-05-17`

本轮已完成：

1. 已在 `packages/belldandy-agent/src/tool-agent.ts` 落地工具调用修复 MVP，收敛为两类高价值场景：
   - `tool_call.arguments` 截断 / 闭合不完整的 JSON 自动修复
   - 连续重复的相同 `tool_name + args` 调用拦截
2. 已把修复能力接入现有执行前解析路径：
   - 旧逻辑中 `safeParseJson()` 解析失败会直接退化为 `{}`，容易把“参数截断”变成“空参数执行”
   - 现已改为 `parseToolCallArguments()`：先尝试直接 JSON.parse，失败后在 `full` 模式下做轻量闭合修复，再安全解析
3. 已把重复调用抑制接入现有 `tool_result` / transcript / failure recovery 链路：
   - 命中连续重复调用时，不再真实执行工具
   - 改为返回 synthetic `tool_result`
   - 继续通过 `buildToolResultPromptDeltas()` 给下一轮模型明确反馈“应基于上一轮工具结果继续，而不是重复调用”
4. 已把修复启用级别接到 provider capability：
   - `jsonReliability=low` → `full`
   - `jsonReliability=medium` → `dedupe`
   - `jsonReliability=high/unknown` → `off`
   这样 DeepSeek 一类 JSON 可靠性较低的模型可自动受益，而 OpenAI/Anthropic 默认不额外启用激进修复。

本轮记忆能力影响检查：

- 通过。本轮没有修改 `MemoryManager`、`memory_search` / `memory_read`、durable extraction、memory summary / evolution、task memory 或长期记忆 schema。
- 修复逻辑只作用于 `ToolEnabledAgent` 的单次工具调用执行前解析与重复调用治理，不会改写静态 `MEMORY.md` 注入、recent memory / auto recall 注入或长期记忆持久化格式。
- 重复调用拦截只生成当前 run 内的 synthetic `tool_result`，不会把 repair 控制状态写入长期记忆数据库。

关键验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-agent/src/tool-agent.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/agent --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 当前实现仅覆盖 `JSON truncation repair + duplicate suppression`，尚未实现完整四遍修复管线、reasoning_content 调用回收或 schema flatten；这些属于后续增强，不阻塞阶段 5 下一项启动。

下一步建议：

1. 正式进入阶段 5 / `B4` 缓存对齐摘要路径。
2. 若后续继续深化 `B3`，优先补 reasoning scavenging，再评估更激进的 storm/flatten pass，不要先扩大修复面。
3. 当前 repair 级别已按 `jsonReliability` 自动治理；若未来需要运维显式开关，再单独评估是否补 env override，而不是现在扩大配置面。

### 6.22 当前推进记录（阶段 5 / B4 缓存对齐摘要）

记录日期：`2026-05-17`

本轮已完成：

1. 已对 `packages/belldandy-agent/src/compaction.ts` 的 summarizer 接口做兼容式扩展：
   - 原 `SummarizerFn(prompt: string)` 继续可用
   - 新增可选 `SummarizerContext`
   - rolling / archival 摘要现在可把结构化上下文一并传给 summarizer，而不是只传一段拼好的 prompt 字符串
2. 已在 Gateway compaction summarizer 中落地 cache-aligned 摘要请求构造 MVP：
   - 当 provider capability `cache=supported` 时
   - 不再只发送单个摘要 prompt
   - 改为“回放原始摘要素材 + 追加摘要指令 + `<<CACHE_BREAK>>`”的请求构造
   - 让缓存能力 provider 更容易命中稳定前缀，而摘要意图只体现在最后一条 user instruction
3. 已把 cache-aligned 构造从 `gateway.ts` 抽到独立 helper：
   - 新增 `packages/belldandy-core/src/compaction-cache-aligned.ts`
   - 包含 rolling / archival 两类 replay 消息与 responses input 的构造逻辑
   - 避免把启动入口里的摘要逻辑继续做厚
4. 非缓存 provider 保持原路径不变：
   - `cache !== supported` 时仍走原有单 prompt 摘要
   - 当前行为对 OpenAI / 无缓存 provider 保持兼容，不额外引入请求体膨胀

本轮记忆能力影响检查：

- 通过。本轮没有修改 `MemoryManager`、`memory_search` / `memory_read`、durable extraction、memory summary / evolution、task memory 或长期记忆 schema。
- `B4` 只改变 compaction summarizer 的请求构造方式，不改变摘要的持久化位置、对话恢复结构或长期记忆写入边界。
- `<<CACHE_BREAK>>` 仅作为 summarizer 请求中的显式提示，不会写入长期记忆数据库，也不会污染运行时正常对话 transcript。

关键验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-agent/src/compaction.test.ts packages/belldandy-core/src/compaction-cache-aligned.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/agent --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 当前只完成 cache-aligned 摘要请求构造，没有补缓存命中率观测、provider 细分策略或 diagnostics 展示；这些属于后续增强，不阻塞阶段 5 下一项启动。

下一步建议：

1. 正式进入阶段 5 / `B1` 工具结果分级压缩。
2. 若后续继续深化 `B4`，优先补 cache hit observability，再评估 Anthropic / DeepSeek 的细分策略，不要先扩大摘要分支复杂度。
3. 当前 `SummarizerFn` 已具备结构化 context 扩展点；后续若要继续做 cache-aware summarizer，不要再把更多上下文塞回单字符串 prompt。

### 6.23 当前推进记录（阶段 5 / B1 工具结果分级压缩）

记录日期：`2026-05-17`

本轮已完成：

1. 已给 `ConversationStore` / `ResidentConversationStore` 增加最近工具结果的独立持久化与查询能力：
   - conversation meta 新增 `recentToolResults`
   - 支持按 `toolCallId`、`toolName`、`success`、`query` 过滤读取
   - 单条结果在落盘前会做长度裁剪，避免恢复链路本身变成新的膨胀点
2. 已在 `ToolEnabledAgent` 把真实与 synthetic `tool_result` 收敛到统一的持久化逻辑：
   - 继续保留原有 `toolDigests`
   - 同步记录最近可恢复的完整/半完整工具结果
   - `before_tool_call` block、hook 失败、duplicate suppression 这类 synthetic failure 也会被记录，避免压缩后失去最近失败原因
3. 已新增只读 builtin `retrieve_tool_result`，并接入 Gateway 工具池：
   - 支持当前会话默认读取，也支持显式 `conversation_id`
   - 支持 `summary / head / tail / full` 四种读取模式
   - 让 `microcompact` / transcript 截断后的工具输出仍有明确恢复入口
4. 本轮保持 `B1` 护栏不变：
   - 默认 `microcompact` 名单仍只覆盖 `run_command` / `file_read` / `list_files` / `web_fetch`
   - `memory_search` / `memory_read` / task memory 相关工具仍不进入默认重压缩名单
   - 会话内压缩与 transcript 持久化继续分离，没有把 `conversation_read` 误当作恢复闭环

本轮记忆能力影响检查：

- 通过。本轮没有修改 `MemoryManager`、`memory.sqlite`、durable extraction、memory summary / evolution、task memory schema 或 `memory_search` / `memory_read` 的返回契约。
- 最近工具结果恢复链路存放在 conversation meta，不写入长期记忆库，也不改变 transcript event schema。
- memory 工具结果仍保持保守策略，未默认纳入重压缩名单；因此“刚刚召回了哪些记忆 / 刚刚读取了哪个 memory 文件”的会话可见性没有因为本轮而下降。

关键验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-agent/src/conversation.test.ts packages/belldandy-agent/src/tool-agent.test.ts packages/belldandy-skills/src/builtin/conversation/conversation-tools.test.ts packages/belldandy-agent/src/microcompact.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/agent --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 当前恢复范围刻意收缩为“最近 N 条工具结果的 conversation meta 级恢复”，尚未扩展到更长期 transcript/export 或跨会话索引；这不阻塞阶段 5 下一项启动。

下一步建议：

1. 进入阶段 5 / `B2` 分层模型路由。
2. 若后续要把 memory 工具结果纳入重压缩，必须继续保留 `retrieve_tool_result` 或等价恢复路径，不能回退到依赖 `conversation_read`。
3. 若后续要继续深化 `B1`，优先补恢复结果的可观测性与更细的保留策略，再评估是否值得把 transcript/export 也扩成可恢复工具结果视图。

### 6.24 当前推进记录（阶段 5 / B2 分层模型路由）

记录日期：`2026-05-17`

本轮已完成：

1. 已新增 compaction 路由解析入口 `resolveCompactionModelRoute(...)`：
   - 支持 `primary`、已配置 fallback profile id、`manual:<model>` 这三类 route ref 语义
   - 兼容显式 `BELLDANDY_COMPACTION_BASE_URL` / `BELLDANDY_COMPACTION_API_KEY` / `BELLDANDY_COMPACTION_MODEL` 覆盖
   - 对缺凭证、缺模型、协议不兼容三类情况返回显式 disabled reason，避免静默落到错误路由
2. 已把 Gateway 的 compaction / summarizer 路径统一接到该 route：
   - `BELLDANDY_COMPACTION_MODEL_ROUTE` 已接入启动配置与 config channel 白名单
   - runtime resilience 的 `routing.compaction` 会暴露当前 compaction 路由、provider、model、wire API 与是否共享主路由
   - compaction summarizer 会按 route 上的 `wireApi` 选择 `responses` 或 `chat_completions`
3. 已收缩首版边界，避免把高风险链路一起放开：
   - 当前只覆盖 compaction / 辅助摘要
   - 不自动扩展到 `MEMORY_SUMMARY_*`、`MEMORY_EVOLUTION_*`、`TASK_SUMMARY_*`
   - `anthropic` compaction route 当前会被显式标记为 `unsupported_protocol`，因为现有摘要请求体仍只支持 OpenAI-compatible 路径
4. 已补齐最小回归验证：
   - helper 单测覆盖 primary / named / explicit override / anthropic disabled
   - `system.doctor` 已验证能透出注入后的 compaction route 元数据
   - `config.update` / `config.read` 已覆盖 `BELLDANDY_COMPACTION_MODEL_ROUTE` 白名单接线

本轮记忆能力影响检查：

- 通过。本轮没有修改 `MemoryManager`、durable extraction、memory summary / evolution 生成链路或 `memory_search` / `memory_read` 返回契约。
- `B2` 首版刻意不接管 `MEMORY_SUMMARY_*`、`MEMORY_EVOLUTION_*`、`TASK_SUMMARY_*`，避免为了省 token 先引入摘要质量、提取质量、误召回率与漏召回率漂移。
- compaction 路由保持可回退、可配置、可逐项关闭；当 route 不兼容当前摘要协议时，会显式禁用而不是偷偷切到未知行为。

关键验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-core/src/compaction-model-routing.test.ts packages/belldandy-core/src/server.doctor.test.ts packages/belldandy-core/src/server.config-channels.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/core build`

当前阻塞：

- 无新增阻塞。
- 当前 compaction 分层路由仍限定为 OpenAI-compatible summarizer；若后续要支持 Anthropic 原生摘要协议，需要单独补请求构造与质量回归，不应在本项里顺手扩范围。

下一步建议：

1. 正式转入阶段 6 / `dedup dry-run`。
2. 若后续继续深化 `B2`，优先把 memory summary / evolution 与 task summary 视为独立子项，分别做质量验证，不要沿用 compaction 的结论直接复制。
3. 若后续需要支持 Anthropic compaction route，先补原生请求体与摘要回归，再决定是否开放配置入口给用户。

### 6.25 当前推进记录（阶段 6 / dedup dry-run）

记录日期：`2026-05-17`

本轮已完成：

1. 已新增只读 exact dedup report helper：
   - 对 `chunks` 表执行本地规范化内容 hash 分组
   - 规范化规则当前收缩为 `BOM 去除 + CRLF/LF 统一 + trim`
   - 每个重复组会返回 `keep / remove / affectedSourcePaths / affectedTaskLinkCount / preview`
2. 已把 dry-run 报告挂到 `MemoryStore` / `MemoryManager`：
   - 只读扫描当前库内 chunk
   - 支持复用现有 `MemorySearchFilter`
   - 不写入 `meta`、不 bump `memory_change_seq`、不修改任何 chunk 可见性或内容
3. 已新增 RPC 入口 `memory.dedup.preview`：
   - 通过现有 memory 通道返回 dedup report
   - 支持 `filter` 与 `maxGroups`
   - 当前定位为运维预检入口，不引入 WebChat 复杂批量操作面
4. 已补齐首轮验证：
   - 单测覆盖 exact duplicate 分组与 keeper 选择
   - 集成测试覆盖 RPC 返回报告且 chunk 总数不变
   - 相关 memory/core build 已通过

本轮记忆能力影响检查：

- 通过。本轮没有执行任何删除、更新、索引 rebuild、向量表重建或备份覆盖操作。
- `memory.search`、durable extraction、experience 流转、shared memory、task memory、context injection 均未修改返回契约。
- dry-run 只输出报告，不把“候选移除项”误表述为已删除结果；因此当前不构成不可逆副作用。

关键验证：

- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-memory/src/memory-dedup.test.ts packages/belldandy-core/src/server.memory-experience.test.ts --reporter verbose`
- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-memory/src/memory-dedup.test.ts packages/belldandy-core/src/server.memory-experience.test.ts packages/belldandy-core/src/server.doctor.test.ts --reporter verbose`
- 通过 `corepack pnpm --filter @belldandy/memory --filter @belldandy/core build`

当前阻塞：

- 当前尚未实现 apply，因此还没有自动备份、事务删除、`task_memory_links` 清理策略、FTS / 向量索引恢复验证。
- 当前 exact 判定只依赖规范化后内容 hash；对“语义近似但文本不完全相同”的重复不会命中，这是刻意收缩范围。

下一步建议：

1. 进入阶段 6 / `dedup apply`，先补自动备份与显式回滚路径，再做实际删除。
2. apply 实现前先明确 keeper 策略与 link 迁移策略，特别是 `task_memory_links` 是否需要迁移到保留 chunk。
3. fuzzy dedup 继续保持默认关闭，不要在 apply 首版里顺手引入辅助模型判重。

### 6.26 当前推进记录（阶段 6 / dedup apply）

记录日期：`2026-05-17`

本轮已完成：

1. 已在 `packages/belldandy-memory/src/memory-dedup.ts` 补齐 apply 规划与备份辅助：
   - `buildMemoryExactDedupApplyPlan(...)`
   - `ensureMemoryDedupBackupFile(...)`
   - apply 结果结构统一为 `mode=apply`
2. 已在 `MemoryStore` / `MemoryManager` 落地 exact dedup apply：
   - 复用 dry-run 的 keeper 选择规则
   - 先创建 `memory.sqlite` 备份
   - 对每个 remove chunk 把 `task_memory_links` 迁移到 keeper
   - 删除重复 chunk，并在存在 `chunks_vec` 时按 `rowid` 删除对应向量
   - 删除完成后 bump `memory_change_seq`，并保守触发 `ensureFtsRebuiltIfNeeded()`
3. 已新增 RPC `memory.dedup.apply`：
   - 强制要求 `confirmed=true`
   - 默认把备份落到 `<stateDir>/artifacts/memory-dedup-backups/`
   - 继续复用现有 `MemorySearchFilter` 与 `maxGroups`
   - 当前确认仍是接口级确认：调用方必须主动传 `confirmed=true`，后端不会自动执行
4. 已补齐首轮验证：
   - `memory-dedup.test.ts` 覆盖备份文件创建
   - `server.memory-experience.test.ts` 覆盖 RPC apply、备份落盘、重复 chunk 删除与 `task_memory_links` 迁移
   - `@belldandy/memory` / `@belldandy/core` build 已恢复为通过

本轮记忆能力影响检查：

- 通过。本轮对 `memory.sqlite` 的写操作被收束在显式确认的 `memory.dedup.apply` 中，默认不会被普通查询路径触发。
- `memory.search`、experience candidate 流转、shared memory、task memory 查询契约未被改写；apply 只影响被判定为 exact duplicate 的 chunk 集合。
- 当前已提供自动备份文件，失败后的最小回滚路径明确：使用本轮生成的 `memory-dedup-<runId>.sqlite` 恢复原库。

关键验证：

- 通过 `corepack pnpm --filter @belldandy/memory --filter @belldandy/core build`
- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-memory/src/memory-dedup.test.ts packages/belldandy-core/src/server.memory-experience.test.ts --reporter verbose`
- 通过 `node .\\node_modules\\vitest\\vitest.mjs run packages/belldandy-memory/src/memory-dedup.test.ts packages/belldandy-core/src/server.memory-experience.test.ts packages/belldandy-core/src/server.doctor.test.ts --reporter verbose`

当前阻塞：

- 无阻塞下一阶段启动的遗留问题。
- 当前 remaining gap 是：`chunks_vec` orphan 删除与 FTS rebuild 虽已在实现中接线，但本轮没有单独构造 vec-enabled 专项断言；这是验证缺口，不影响 exact apply 首版收口。
- WebChat 第 3/4 步现已补齐：
  - 入口位置为 `记忆查看 -> 记忆条目` 工具栏中的 `重复预检`
  - dry-run 结果 modal 中已提供 `确认清理` 按钮
  - 前端会在用户确认后调用 `memory.dedup.apply`，并显式传 `confirmed=true`
  - 本轮已用本地隔离 state dir 完成真实页面 smoke：预检报告可展示 exact duplicate 组，确认清理后会显示备份路径并刷新列表统计
- fuzzy / semantic dedup 仍未实现，这是刻意收缩范围，不应视为本项缺陷。

下一步建议：

1. 若继续深化阶段 6，优先补 vec-enabled 专项回归，而不是直接引入 fuzzy 判重。
2. 后续若继续深化人工运维体验，优先补更细的风险提示与 vec-enabled 诊断，而不是再新增新的 dedup 入口：
   - 当前 WebChat 已具备 dry-run 报告、二次确认、apply 触发与备份路径展示
   - 后续更值得补的是 vec/FTS 状态观测、回滚说明和更细粒度的筛选提示
3. fuzzy dedup 建议单列子项处理，先定义误删风险与回滚策略，再评估是否引入辅助模型。

## 7. 轻量行为验收描述

### 验收 A：记忆总开关

Given 已启用 `INJECT_AGENTS` / `INJECT_SOUL` / `INJECT_MEMORY`
When 设置 `BELLDANDY_MEMORY_ENABLED=false` 并重启 Gateway
Then 动态记忆注入与记忆写入关闭，但静态 workspace prompt 仍可正常注入

### 验收 B：Commander 硬约束

Given 当前会话由 `commander` Agent 执行
When 模型尝试获取写文件或命令执行类工具
Then 工具定义阶段直接不可见，Commander 无法越权调用

### 验收 C：Prompt 聚焦

Given `AGENTS.md` / `SOUL.md` 中存在与当前任务高度相关的章节
When 用户输入新任务
Then `before_agent_start` 可注入少量相关提示 delta，且静态 system prompt 缓存前缀不被重写

### 验收 D：Scratch Memory

Given Commander 已委派执行者完成一个 Goal Node
When 执行者在 run 中产生中间假设、错误摘要与排查记录
Then 这些内容写入 `scratch-{agentId}.md`，不会进入长期记忆数据库

---

## 8. 明确延期项

以下内容建议 `defer`，不进入首轮主实施范围：

1. 云端记忆后端接入
2. 全自动 fuzzy dedup
3. Commander 复杂 auto 决策引擎
4. 子智能体蒸馏指标
5. 完整审计日志压缩体系

延期原因：

- 这些项依赖前面阶段的边界先稳定。
- 过早推进会扩大设计面并增加返工概率。

---

## 9. 收口边界

本计划的“完成”不等于三份文档所有想法一次性全部实现，而是指：

- 已形成明确阶段边界；
- 已排序前置依赖；
- 已识别冲突点；
- 已将低风险基础设施、治理闭环、Prompt 优化、资产统一、记忆清理拆成可执行阶段；
- 每阶段都有风险、工作量、验证与延期边界。

后续实施时，建议按阶段立项与验收，不建议把三份文档内容作为一个超大任务同时开工。
